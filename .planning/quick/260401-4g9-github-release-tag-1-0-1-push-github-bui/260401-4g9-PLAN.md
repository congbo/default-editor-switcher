---
description: 再次删除 GitHub release/tag 并重新发布 v1.0-preview.1
date: 2026-04-01
status: completed
---

# Quick Task 260401-4g9 Plan

1. Delete the existing `v1.0-preview.1` GitHub prerelease plus matching local and remote tags so the preview can be recreated cleanly.
2. Verify the Xcode project still resolves to `MARKETING_VERSION = 1.0` and `CURRENT_PROJECT_VERSION = 1`, with no source changes required before release.
3. Rebuild the preview artifact, verify the manifest and archive resolve to `v1.0-preview.1`, republish the GitHub prerelease, and record the quick-task artifacts in `.planning/STATE.md`.

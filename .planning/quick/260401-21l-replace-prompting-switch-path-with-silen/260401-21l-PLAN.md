---
description: Replace prompting switch path with silent Launch Services mutation
date: 2026-03-31
status: completed
---

# Quick Task 260401-21l Plan

1. Replace the global text switch write path so it preflights editor eligibility, mutates the user Launch Services plist in one batch, reloads Launch Services once, and verifies results by readback instead of per-type API writes.
2. Extend switch failure reporting so silent-path plist/reload failures surface recovery diagnostics without falling back to prompting system APIs.
3. Add focused unit coverage for the new coordinator flow and plist upsert behavior, then run the targeted macOS test suite.

---
description: 再次删除 GitHub release/tag，保持版本号 1.0 (1)，重新发布 v1.0-preview.1
date: 2026-03-30
status: completed
---

# Quick Task 260330-ve9 Plan

1. Delete the existing `v1.0-preview.1` GitHub prerelease plus local and remote tag so the preview can be recreated cleanly.
2. Confirm the project remains at `MARKETING_VERSION = 1.0` and `CURRENT_PROJECT_VERSION = 1`, and push the current `master` state to GitHub.
3. Rebuild the preview artifact, verify the manifest/archive still resolve to `v1.0-preview.1`, republish the GitHub prerelease, and record the task in `.planning/quick` and `.planning/STATE.md`.

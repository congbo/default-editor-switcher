---
description: 修正推荐编辑器设置与菜单栏不同步、取消一级菜单补齐逻辑，并更新默认推荐与相关文档
date: 2026-03-26
status: completed
---

# Quick Task 260326-5rt Plan

1. Make recommended-menu persistence publish changes only after configuration is saved, and block disabling the final checked editor.
2. Update known-editor defaults so the ordered catalog includes `Qoder` after `Zed`, the default enabled set includes `TextEdit`, and the first-level menu follows checked available recommendations only.
3. Refresh settings copy, unit tests, README text, and Phase 02/03 planning artifacts so they match the new first-level-vs-`More` behavior.

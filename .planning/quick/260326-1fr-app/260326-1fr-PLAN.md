---
quick_id: 260326-1fr
description: 调整非全局推荐 app 的默认排序，按支持的文本扩展名数量降序排列
created: 2026-03-26
status: complete
---

# Quick Task 260326-1fr Plan

## Goal

Keep curated recommendations fixed at the front of the global menu, but make non-curated apps sort by how many developer-text extensions they declare support for.

## Tasks

1. Add a supported-text-extension count to discovered editor candidates.
2. Update the global ranking policy so non-curated apps sort by that count in descending order.
3. Add regression tests and record the clarified behavior in Phase 2 docs and project state.

---
phase: 03-native-settings-window
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - DefaultEditorSwitcher.xcodeproj/project.pbxproj
  - App/Application/Settings/RecommendedMenuAppsStore.swift
  - App/Features/Settings/RecommendedAppsSettingsSection.swift
  - App/Features/MenuBar/MenuBarViewModel.swift
  - App/Domain/Editors/EditorRankingPolicy.swift
  - App/Support/KnownEditors.swift
  - Tests/DefaultEditorSwitcherTests/RecommendedMenuAppsStoreTests.swift
  - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift
  - Tests/DefaultEditorSwitcherTests/EditorRankingPolicyTests.swift
autonomous: true
requirements:
  - MENU-03
  - DISC-01
  - DISC-03
user_setup: []
must_haves:
  truths:
    - The first-level menu recommended rows follow a persisted user-configured order and inclusion set instead of a fully hardcoded catalog order.
    - The default stored recommendation list is seeded from the app's existing curated global recommendation order so current users do not get a surprise reset.
    - The top-level menu shows only checked and available full-support recommendations, while unchecked editors move to `More` without backfill or current-editor injection.
  artifacts:
    - App/Application/Settings/RecommendedMenuAppsStore.swift
    - App/Features/Settings/RecommendedAppsSettingsSection.swift
    - App/Features/MenuBar/MenuBarViewModel.swift
    - Tests/DefaultEditorSwitcherTests/RecommendedMenuAppsStoreTests.swift
    - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift
  key_links:
    - RecommendedMenuAppsStore persists ordered bundle IDs and resolves them against discovered eligible apps.
    - MenuBarViewModel consumes RecommendedMenuAppsStore before calculating primary and overflow rows.
    - KnownEditors remains the immutable default catalog and only seeds the initial recommendation order.
---

<objective>
Add configurable recommended-app settings and make the menu bar consume those stored preferences without regressing the quick-switch flow.

Purpose: The user asked to control which apps show up as first-level menu recommendations, and that behavior must remain consistent with the existing menu ranking guarantees.
Output: A persisted recommended-app preference store, a native settings section for selection and ordering, and menu-bar integration that honors the stored order while limiting the first level to checked available recommendations.
</objective>

<execution_context>
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-native-settings-window/03-RESEARCH.md
@.planning/phases/03-native-settings-window/03-VALIDATION.md
@.planning/phases/03-native-settings-window/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist recommended-app configuration seeded from the existing curated order</name>
  <files>App/Application/Settings/RecommendedMenuAppsStore.swift, App/Support/KnownEditors.swift</files>
  <read_first>App/Support/KnownEditors.swift, App/Domain/Editors/EditorRankingPolicy.swift, App/Features/MenuBar/MenuBarViewModel.swift, .planning/phases/03-native-settings-window/03-RESEARCH.md</read_first>
  <action>Create `App/Application/Settings/RecommendedMenuAppsStore.swift` with persisted fields for `orderedBundleIDs`, `enabledBundleIDs`, and an initializer that seeds both from `KnownEditors.catalog.map(\\.bundleID)` when no user defaults exist. Add explicit APIs `loadConfiguration()`, `setEnabled(bundleID:isEnabled:)`, `move(bundleID:beforeBundleID:)`, and `resolvedRecommendedBundleIDs(availableBundleIDs:)`. Update `App/Support/KnownEditors.swift` to expose `defaultRecommendedBundleIDs` and `defaultRecommendedEditors` helpers without mutating the existing immutable catalog.</action>
  <verify>rg -n "orderedBundleIDs|enabledBundleIDs|loadConfiguration|setEnabled\\(|move\\(|resolvedRecommendedBundleIDs|defaultRecommendedBundleIDs" App/Application/Settings/RecommendedMenuAppsStore.swift App/Support/KnownEditors.swift</verify>
  <acceptance_criteria>
    - App/Application/Settings/RecommendedMenuAppsStore.swift contains "orderedBundleIDs" and "enabledBundleIDs"
    - RecommendedMenuAppsStore.swift seeds defaults from `KnownEditors.catalog.map(\.bundleID)` or an equivalent helper
    - App/Support/KnownEditors.swift contains `defaultRecommendedBundleIDs`
    - RecommendedMenuAppsStore resolves stored IDs against currently available bundle IDs
  </acceptance_criteria>
  <done>The app now has a persisted recommendation overlay that starts from the existing curated defaults instead of inventing a new order.</done>
</task>

<task type="auto">
  <name>Task 2: Make the menu bar honor configured recommended apps without backfill or current-editor injection</name>
  <files>App/Features/MenuBar/MenuBarViewModel.swift, App/Domain/Editors/EditorRankingPolicy.swift</files>
  <read_first>App/Features/MenuBar/MenuBarViewModel.swift, App/Domain/Editors/EditorRankingPolicy.swift, App/Application/Settings/RecommendedMenuAppsStore.swift, .planning/phases/03-native-settings-window/03-RESEARCH.md</read_first>
  <action>Update `App/Features/MenuBar/MenuBarViewModel.swift` so primary-row construction consumes only `RecommendedMenuAppsStore.resolvedRecommendedBundleIDs(availableBundleIDs:)`, maps those bundle IDs back to full-support candidates in the persisted order, and leaves every unchecked or unavailable candidate in overflow. Do not backfill to 12 or inject the current editor into the first-level menu. Update `App/Domain/Editors/EditorRankingPolicy.swift` so catalog order remains the default only when no stored recommendation override is supplied; once a recommendation override exists, use that explicit bundle-ID order before falling back to weight and display-name comparisons.</action>
  <verify>rg -n "primaryMenuLimit|resolvedRecommendedBundleIDs|current editor|currentBundleID|12|recommendation override" App/Features/MenuBar/MenuBarViewModel.swift App/Domain/Editors/EditorRankingPolicy.swift</verify>
  <acceptance_criteria>
    - App/Features/MenuBar/MenuBarViewModel.swift calls `resolvedRecommendedBundleIDs`
    - MenuBarViewModel.swift builds the first level only from resolved recommended bundle IDs
    - MenuBarViewModel.swift leaves unchecked or current-but-unchecked editors outside the first-level list
    - App/Domain/Editors/EditorRankingPolicy.swift keeps a default-path fallback when no override is present
  </acceptance_criteria>
  <done>The first-level menu recommendation behavior becomes user-configurable without losing the existing quick-switch safeguards.</done>
</task>

<task type="auto">
  <name>Task 3: Add a native recommended-app settings section and regression tests</name>
  <files>DefaultEditorSwitcher.xcodeproj/project.pbxproj, App/Features/Settings/RecommendedAppsSettingsSection.swift, Tests/DefaultEditorSwitcherTests/RecommendedMenuAppsStoreTests.swift, Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift, Tests/DefaultEditorSwitcherTests/EditorRankingPolicyTests.swift</files>
  <read_first>App/Features/Settings/SettingsWindowView.swift, App/Application/Settings/RecommendedMenuAppsStore.swift, App/Features/MenuBar/MenuBarViewModel.swift, Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift, .planning/phases/03-native-settings-window/03-VALIDATION.md</read_first>
  <action>Create `App/Features/Settings/RecommendedAppsSettingsSection.swift` as a native section that lists discovered candidate apps with checkboxes or toggles for inclusion and explicit move controls (`Move Up`, `Move Down`) or drag-based reorder state, plus helper copy explaining that checked editors appear in the first-level menu and unchecked ones move to `More`. Add `Tests/DefaultEditorSwitcherTests/RecommendedMenuAppsStoreTests.swift` with coverage for default seeding, persistence round-trip, minimum-one enablement, post-persist publishing, and reorder operations. Extend `Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift` with tests proving live menu refresh, no-backfill first-level behavior, and current-editor overflow behavior. Extend `Tests/DefaultEditorSwitcherTests/EditorRankingPolicyTests.swift` with a test proving explicit recommendation override order beats the default catalog order.</action>
  <verify>rg -n "Move Up|Move Down|first-level menu|testPrimaryRowsRespectConfiguredRecommendedAppOrder|override order" App/Features/Settings/RecommendedAppsSettingsSection.swift Tests/DefaultEditorSwitcherTests/RecommendedMenuAppsStoreTests.swift Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift Tests/DefaultEditorSwitcherTests/EditorRankingPolicyTests.swift</verify>
  <acceptance_criteria>
    - App/Features/Settings/RecommendedAppsSettingsSection.swift contains `Move Up` and `Move Down` or equivalent native reorder controls
    - Tests/DefaultEditorSwitcherTests/RecommendedMenuAppsStoreTests.swift exists
    - RecommendedMenuAppsStoreTests.swift contains default-seeding and reorder assertions
    - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift contains `testPrimaryRowsRespectConfiguredRecommendedAppOrder`
    - Tests/DefaultEditorSwitcherTests/EditorRankingPolicyTests.swift verifies override order beats default catalog order
  </acceptance_criteria>
  <done>The recommendation feature is exposed through native settings UI and backed by tests that prove the menu follows the stored configuration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "defaultRecommendedBundleIDs|orderedBundleIDs|enabledBundleIDs" App/Application/Settings/RecommendedMenuAppsStore.swift App/Support/KnownEditors.swift` succeeds
- [ ] `rg -n "resolvedRecommendedBundleIDs|primaryMenuLimit|testPrimaryRowsRespectConfiguredRecommendedAppOrder" App/Features/MenuBar/MenuBarViewModel.swift Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift` succeeds
- [ ] `xcodebuild test -scheme DefaultEditorSwitcher -destination 'platform=macOS' -only-testing:DefaultEditorSwitcherTests/RecommendedMenuAppsStoreTests -only-testing:DefaultEditorSwitcherTests/MenuBarViewModelTests -only-testing:DefaultEditorSwitcherTests/EditorRankingPolicyTests` succeeds
</verification>

<success_criteria>

- All tasks completed
- Recommended first-level menu apps are user-configurable and seeded from the old curated defaults
- The top-level menu follows checked available recommendations only, and unchecked/current editors fall back to overflow
- The settings window exposes recommendation ordering and inclusion through native controls
  </success_criteria>

<output>
After completion, create `.planning/phases/03-native-settings-window/03-02-SUMMARY.md`
</output>

---
phase: 03-native-settings-window
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - DefaultEditorSwitcher.xcodeproj/project.pbxproj
  - App/DefaultEditorSwitcherApp.swift
  - App/Application/Localization/AppLanguage.swift
  - App/Application/Localization/AppLanguageStore.swift
  - App/Features/Settings/LanguageSettingsSection.swift
  - App/Features/Settings/SettingsWindowView.swift
  - App/Features/MenuBar/MenuBarContentView.swift
  - App/Features/MenuBar/MenuBarViewModel.swift
  - App/Features/Settings/GeneralSettingsSection.swift
  - App/Features/Settings/RecommendedAppsSettingsSection.swift
  - App/Resources/Localizable.xcstrings
  - Tests/DefaultEditorSwitcherTests/AppLanguageStoreTests.swift
  - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - The app exposes a stored language preference with follow-system as the default and English/Chinese as explicit overrides.
    - The settings window and menu bar use real localization resources so app-owned copy follows the selected language.
    - Language preference integration does not replace the menu bar or settings architecture built in Plans 01 and 02; it layers on top of them.
  artifacts:
    - App/Application/Localization/AppLanguage.swift
    - App/Application/Localization/AppLanguageStore.swift
    - App/Features/Settings/LanguageSettingsSection.swift
    - App/Resources/Localizable.xcstrings
    - Tests/DefaultEditorSwitcherTests/AppLanguageStoreTests.swift
  key_links:
    - DefaultEditorSwitcherApp injects the selected app-language locale into the menu and settings scenes.
    - MenuBarViewModel and menu/settings views stop owning hardcoded user-facing literals that should be localized.
    - LanguageSettingsSection edits AppLanguageStore, not raw user defaults keys inside view code.
---

<objective>
Localize the app-owned menu and settings copy and add app-language switching with follow-system, English, and Chinese options.

Purpose: The settings window must include app language selection, and that preference only feels real if the app's visible copy actually follows it.
Output: A persisted `AppLanguage` preference, locale injection at the app root, localized menu/settings strings in a string catalog, and tests proving the preference and menu labels follow the selected language.
</objective>

<execution_context>
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-native-settings-window/03-RESEARCH.md
@.planning/phases/03-native-settings-window/03-VALIDATION.md
@.planning/phases/03-native-settings-window/03-01-PLAN.md
@.planning/phases/03-native-settings-window/03-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add persisted app-language state and root locale injection</name>
  <files>DefaultEditorSwitcher.xcodeproj/project.pbxproj, App/DefaultEditorSwitcherApp.swift, App/Application/Localization/AppLanguage.swift, App/Application/Localization/AppLanguageStore.swift, App/Features/Settings/LanguageSettingsSection.swift</files>
  <read_first>App/DefaultEditorSwitcherApp.swift, App/Features/Settings/SettingsWindowView.swift, .planning/phases/03-native-settings-window/03-RESEARCH.md, .planning/phases/03-native-settings-window/03-VALIDATION.md</read_first>
  <action>Create `App/Application/Localization/AppLanguage.swift` with an enum containing exactly `.system`, `.english`, and `.simplifiedChinese`, plus helpers for localized display labels and optional locale identifiers `nil`, `en`, and `zh-Hans`. Create `App/Application/Localization/AppLanguageStore.swift` that persists the selected enum in `UserDefaults`, exposes `selectedLanguage`, and computes an optional `Locale` override for SwiftUI scene injection. Update `App/DefaultEditorSwitcherApp.swift` to own one shared `AppLanguageStore` and inject the selected locale override into both the menu-bar scene and `settings-window` scene. Implement `App/Features/Settings/LanguageSettingsSection.swift` with a native `Picker` that offers `Follow System`, `English`, and `简体中文` in that exact order, defaulting to `.system`.</action>
  <verify>rg -n "case system|case english|case simplifiedChinese|zh-Hans|Follow System|English|简体中文|\\.environment\\(\\\\.locale" App/Application/Localization/AppLanguage.swift App/Application/Localization/AppLanguageStore.swift App/Features/Settings/LanguageSettingsSection.swift App/DefaultEditorSwitcherApp.swift</verify>
  <acceptance_criteria>
    - App/Application/Localization/AppLanguage.swift contains `.system`, `.english`, and `.simplifiedChinese`
    - App/Application/Localization/AppLanguageStore.swift persists the selected language in `UserDefaults`
    - App/Features/Settings/LanguageSettingsSection.swift contains `Follow System`, `English`, and `简体中文`
    - App/DefaultEditorSwitcherApp.swift injects `.environment(\.locale, ...)` or an equivalent locale override into both scenes
  </acceptance_criteria>
  <done>The app has a real language preference model and a root-level locale override pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Move app-owned menu and settings copy into localization resources</name>
  <files>DefaultEditorSwitcher.xcodeproj/project.pbxproj, App/Features/MenuBar/MenuBarContentView.swift, App/Features/MenuBar/MenuBarViewModel.swift, App/Features/Settings/SettingsWindowView.swift, App/Features/Settings/GeneralSettingsSection.swift, App/Features/Settings/RecommendedAppsSettingsSection.swift, App/Features/Settings/LanguageSettingsSection.swift, App/Resources/Localizable.xcstrings</files>
  <read_first>App/Features/MenuBar/MenuBarContentView.swift, App/Features/MenuBar/MenuBarViewModel.swift, App/Features/Settings/SettingsWindowView.swift, App/Features/Settings/GeneralSettingsSection.swift, App/Features/Settings/RecommendedAppsSettingsSection.swift, App/Features/Settings/LanguageSettingsSection.swift, .planning/phases/03-native-settings-window/03-RESEARCH.md</read_first>
  <action>Replace hardcoded app-owned English literals in the menu and settings flows with localizable keys backed by `App/Resources/Localizable.xcstrings`. Cover at least these strings: `Settings...`, `Quit Default Editor Switcher`, `General`, `Menu Bar`, `Language`, `Launch at login`, the recommended-app explanatory copy, and the summary fallback strings currently emitted from `MenuBarViewModel` such as `Loading...`, `No Global Editor Detected`, and `Mixed Defaults`. Keep editor display names and bundle IDs unlocalized. Ensure `MenuBarViewModel` no longer hardcodes user-facing fallback copy that should track app language.</action>
  <verify>rg -n "LocalizedStringKey|String\\(localized:|Loading\\.\\.\\.|No Global Editor Detected|Mixed Defaults|Settings\\.\\.\\.|Quit Default Editor Switcher" App/Features/MenuBar/MenuBarContentView.swift App/Features/MenuBar/MenuBarViewModel.swift App/Features/Settings/SettingsWindowView.swift App/Resources/Localizable.xcstrings</verify>
  <acceptance_criteria>
    - App/Resources/Localizable.xcstrings exists
    - Localizable.xcstrings includes entries for `Settings...`, `General`, `Menu Bar`, and `Language`
    - App/Features/MenuBar/MenuBarViewModel.swift no longer relies on only hardcoded English fallback strings for loading and mixed/unavailable summaries
    - App/Features/MenuBar/MenuBarContentView.swift localizes `Quit Default Editor Switcher`
  </acceptance_criteria>
  <done>The app's visible menu and settings copy is routed through real localization resources instead of ad hoc literals.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests proving language preference persistence and localized menu labels</name>
  <files>Tests/DefaultEditorSwitcherTests/AppLanguageStoreTests.swift, Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift</files>
  <read_first>App/Application/Localization/AppLanguage.swift, App/Application/Localization/AppLanguageStore.swift, App/Features/MenuBar/MenuBarViewModel.swift, App/Resources/Localizable.xcstrings, .planning/phases/03-native-settings-window/03-VALIDATION.md</read_first>
  <action>Create `Tests/DefaultEditorSwitcherTests/AppLanguageStoreTests.swift` with assertions for default `.system`, persistence round-trip to `.english` and `.simplifiedChinese`, and locale mapping to `nil`, `en`, and `zh-Hans`. Extend `Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift` with a test named `testLocalizedMenuLabelsFollowSelectedAppLanguage()` that injects a deterministic language/localization dependency and proves menu-owned copy such as the settings action title and fallback summary strings change when the selected app language changes.</action>
  <verify>rg -n "testLocalizedMenuLabelsFollowSelectedAppLanguage|zh-Hans|simplifiedChinese|english|system" Tests/DefaultEditorSwitcherTests/AppLanguageStoreTests.swift Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift</verify>
  <acceptance_criteria>
    - Tests/DefaultEditorSwitcherTests/AppLanguageStoreTests.swift exists
    - AppLanguageStoreTests.swift verifies default `.system` and persistence round-trips for `.english` and `.simplifiedChinese`
    - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift contains `testLocalizedMenuLabelsFollowSelectedAppLanguage`
    - MenuBarViewModelTests proves at least one menu label and one fallback summary string change with the selected app language
  </acceptance_criteria>
  <done>The language-selection feature is backed by repeatable tests instead of only manual UI checking.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "case system|case english|case simplifiedChinese|zh-Hans" App/Application/Localization/AppLanguage.swift App/Application/Localization/AppLanguageStore.swift` succeeds
- [ ] `rg -n "Localizable\\.xcstrings|Settings\\.\\.\\.|Quit Default Editor Switcher|testLocalizedMenuLabelsFollowSelectedAppLanguage" App/Resources/Localizable.xcstrings App/Features/MenuBar/MenuBarContentView.swift Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift` succeeds
- [ ] `xcodebuild test -scheme DefaultEditorSwitcher -destination 'platform=macOS' -only-testing:DefaultEditorSwitcherTests/AppLanguageStoreTests -only-testing:DefaultEditorSwitcherTests/MenuBarViewModelTests` succeeds
</verification>

<success_criteria>

- All tasks completed
- The app exposes follow-system, English, and Chinese language options with follow-system as the default
- Menu and settings strings follow the selected app language through real localization resources
- Language preference changes do not break the resident menu-bar flow or settings architecture
  </success_criteria>

<output>
After completion, create `.planning/phases/03-native-settings-window/03-03-SUMMARY.md`
</output>

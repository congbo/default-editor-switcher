---
phase: 03-native-settings-window
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DefaultEditorSwitcher.xcodeproj/project.pbxproj
  - App/DefaultEditorSwitcherApp.swift
  - App/Application/Startup/LaunchAtLoginService.swift
  - App/Application/Startup/GeneralSettingsViewModel.swift
  - App/Features/Settings/SettingsWindowView.swift
  - App/Features/Settings/GeneralSettingsSection.swift
  - App/Features/MenuBar/MenuBarViewModel.swift
  - Tests/DefaultEditorSwitcherTests/LaunchAtLoginServiceTests.swift
  - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift
autonomous: true
requirements:
  - DIST-03
  - PROD-02
user_setup: []
must_haves:
  truths:
    - The existing placeholder settings entry becomes a real native settings window with production-owned structure rather than placeholder copy.
    - Launch at login is controlled through a dedicated wrapper around `SMAppService.mainApp`, with explicit status reads and failure handling.
    - The menu bar continues to expose a stable `Settings...` action that opens the new window without changing the resident-utility behavior.
  artifacts:
    - App/DefaultEditorSwitcherApp.swift
    - App/Application/Startup/LaunchAtLoginService.swift
    - App/Application/Startup/GeneralSettingsViewModel.swift
    - App/Features/Settings/SettingsWindowView.swift
    - App/Features/Settings/GeneralSettingsSection.swift
    - Tests/DefaultEditorSwitcherTests/LaunchAtLoginServiceTests.swift
  key_links:
    - DefaultEditorSwitcherApp owns the resident menu scene and the stable settings-window scene entry point.
    - GeneralSettingsViewModel mediates between SwiftUI controls and LaunchAtLoginService instead of calling ServiceManagement directly from view bindings.
    - MenuBarViewModel keeps the `Settings...` action stable while the underlying window implementation changes from placeholder to real settings UI.
---

<objective>
Build the real native settings window shell and wire in launch-at-login behavior as the first completed settings feature.

Purpose: Phase 3 needs a production settings foundation before recommendation customization and language switching can land cleanly.
Output: A real settings window scene rooted in native SwiftUI views, a launch-at-login service wrapper around `SMAppService.mainApp`, and tests proving the settings entry point and startup toggle behavior.
</objective>

<execution_context>
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-native-settings-window/03-RESEARCH.md
@.planning/phases/03-native-settings-window/03-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the placeholder rules window with a real settings window shell</name>
  <files>DefaultEditorSwitcher.xcodeproj/project.pbxproj, App/DefaultEditorSwitcherApp.swift, App/Features/Settings/SettingsWindowView.swift, App/Features/Settings/GeneralSettingsSection.swift, App/Features/MenuBar/MenuBarViewModel.swift</files>
  <read_first>App/DefaultEditorSwitcherApp.swift, App/Features/MenuBar/MenuBarContentView.swift, App/Features/MenuBar/RulesWindowPlaceholderView.swift, App/Features/MenuBar/MenuBarViewModel.swift, .planning/phases/03-native-settings-window/03-RESEARCH.md</read_first>
  <action>Update `App/DefaultEditorSwitcherApp.swift` so the secondary scene opened from the menu is a real settings window with a stable ID `settings-window` instead of the Phase 2 placeholder rules window flow. Replace `App/Features/MenuBar/RulesWindowPlaceholderView.swift` usage with a new `App/Features/Settings/SettingsWindowView.swift` root that renders native SwiftUI sections using `Form`, `Section`, `Toggle`, `Picker`, `Button`, and explanatory `Text`. Keep the menu action title as `Settings...` in `MenuBarViewModel`, but rename the backing scene/window identifiers from `rules-window` to `settings-window` everywhere. `SettingsWindowView` must include three visible sections in this exact order: `General`, `Menu Bar`, `Language`, with the latter two allowed to be disabled placeholders until Plans 02 and 03 fill them in.</action>
  <verify>rg -n "settings-window|Settings\\.{3}|Section\\(\"General\"\\)|Section\\(\"Menu Bar\"\\)|Section\\(\"Language\"\\)" App/DefaultEditorSwitcherApp.swift App/Features/Settings/SettingsWindowView.swift App/Features/MenuBar/MenuBarViewModel.swift</verify>
  <acceptance_criteria>
    - App/DefaultEditorSwitcherApp.swift contains "settings-window"
    - App/Features/Settings/SettingsWindowView.swift contains `Section("General")`
    - App/Features/Settings/SettingsWindowView.swift contains `Section("Menu Bar")`
    - App/Features/Settings/SettingsWindowView.swift contains `Section("Language")`
    - App/Features/MenuBar/MenuBarViewModel.swift contains `title: "Settings..."`
  </acceptance_criteria>
  <done>The app has a real native settings window shell instead of a placeholder-only advanced-rules window.</done>
</task>

<task type="auto">
  <name>Task 2: Implement launch-at-login service and general-settings state</name>
  <files>App/Application/Startup/LaunchAtLoginService.swift, App/Application/Startup/GeneralSettingsViewModel.swift, App/Features/Settings/GeneralSettingsSection.swift</files>
  <read_first>App/DefaultEditorSwitcherApp.swift, .planning/phases/03-native-settings-window/03-RESEARCH.md, .planning/phases/03-native-settings-window/03-VALIDATION.md</read_first>
  <action>Create `App/Application/Startup/LaunchAtLoginService.swift` that wraps `SMAppService.mainApp.status`, `register()`, and `unregister()` behind a protocol-friendly API such as `currentStatus()` and `setEnabled(_:) throws`. Create `App/Application/Startup/GeneralSettingsViewModel.swift` with explicit published state for `isEnabled`, `isBusy`, and `errorMessage`, plus `loadStatus()` and `setLaunchAtLoginEnabled(_:)` methods that re-read service state after every toggle attempt. Update `App/Features/Settings/GeneralSettingsSection.swift` to render a native `Toggle("Launch at login", isOn: ...)`, disabled while busy, with secondary explanatory copy and inline error text when the service throws. Do not call `SMAppService` directly from the SwiftUI toggle binding.</action>
  <verify>rg -n "SMAppService\\.mainApp|currentStatus|setEnabled\\(|loadStatus|setLaunchAtLoginEnabled|Toggle\\(\"Launch at login\"" App/Application/Startup/LaunchAtLoginService.swift App/Application/Startup/GeneralSettingsViewModel.swift App/Features/Settings/GeneralSettingsSection.swift</verify>
  <acceptance_criteria>
    - App/Application/Startup/LaunchAtLoginService.swift contains "SMAppService.mainApp"
    - App/Application/Startup/GeneralSettingsViewModel.swift contains "loadStatus" and "setLaunchAtLoginEnabled"
    - App/Features/Settings/GeneralSettingsSection.swift contains `Toggle("Launch at login"`
    - GeneralSettingsViewModel re-reads current service state after a toggle attempt
  </acceptance_criteria>
  <done>The launch-at-login feature is implemented through a testable service boundary and a native General settings section.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for launch-at-login behavior and settings-window action wiring</name>
  <files>Tests/DefaultEditorSwitcherTests/LaunchAtLoginServiceTests.swift, Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift</files>
  <read_first>App/Application/Startup/LaunchAtLoginService.swift, App/Application/Startup/GeneralSettingsViewModel.swift, App/Features/MenuBar/MenuBarViewModel.swift, .planning/phases/03-native-settings-window/03-VALIDATION.md</read_first>
  <action>Create `Tests/DefaultEditorSwitcherTests/LaunchAtLoginServiceTests.swift` with fakes or protocol-backed stubs that prove `setEnabled(true)` calls the register path, `setEnabled(false)` calls the unregister path, and a thrown registration error is surfaced by `GeneralSettingsViewModel`. Update `Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift` by renaming `testOpenRulesWindowActionIsExposed()` to `testOpenSettingsWindowActionIsExposed()` and asserting the window ID is `settings-window` while the visible action title remains `Settings...`.</action>
  <verify>rg -n "testOpenSettingsWindowActionIsExposed|settings-window|register|unregister" Tests/DefaultEditorSwitcherTests/LaunchAtLoginServiceTests.swift Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift</verify>
  <acceptance_criteria>
    - Tests/DefaultEditorSwitcherTests/LaunchAtLoginServiceTests.swift exists
    - LaunchAtLoginServiceTests.swift contains a register-path assertion and an unregister-path assertion
    - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift contains `testOpenSettingsWindowActionIsExposed`
    - MenuBarViewModelTests.swift asserts `windowID == "settings-window"`
  </acceptance_criteria>
  <done>The settings shell and launch-at-login path are protected by repeatable tests before later settings features build on them.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "settings-window" App/DefaultEditorSwitcherApp.swift App/Features/MenuBar/MenuBarViewModel.swift` succeeds
- [ ] `rg -n "SMAppService\\.mainApp|Toggle\\(\"Launch at login\"" App/Application/Startup/LaunchAtLoginService.swift App/Features/Settings/GeneralSettingsSection.swift` succeeds
- [ ] `xcodebuild test -scheme DefaultEditorSwitcher -destination 'platform=macOS' -only-testing:DefaultEditorSwitcherTests/LaunchAtLoginServiceTests -only-testing:DefaultEditorSwitcherTests/MenuBarViewModelTests` succeeds
</verification>

<success_criteria>

- All tasks completed
- The app opens a real settings window instead of a placeholder rules window
- Launch at login is controlled through a dedicated `SMAppService` wrapper rather than inline UI calls
- The menu bar still exposes a stable `Settings...` action while remaining a resident utility
  </success_criteria>

<output>
After completion, create `.planning/phases/03-native-settings-window/03-01-SUMMARY.md`
</output>

---
phase: 02-menu-bar-global-switch
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - App/DefaultEditorSwitcherApp.swift
  - App/Features/MenuBar/MenuBarContentView.swift
  - App/Features/MenuBar/MenuBarViewModel.swift
  - App/Features/MenuBar/RulesWindowPlaceholderView.swift
  - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift
autonomous: true
requirements:
  - MENU-03
  - DIST-03
user_setup: []
must_haves:
  truths:
    - The menu keeps feedback about the most recent switch attempt inside the menu itself, without opening the main window.
    - The menu exposes an explicit refresh cycle and inline current-editor marker so users can trust what they see after a switch.
    - The app can open a separate rules-window placeholder while remaining a menu bar utility.
  artifacts:
    - App/Features/MenuBar/MenuBarContentView.swift
    - App/Features/MenuBar/MenuBarViewModel.swift
    - App/Features/MenuBar/RulesWindowPlaceholderView.swift
    - App/DefaultEditorSwitcherApp.swift
    - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift
  key_links:
    - MenuBarContentView renders feedback from the latest GlobalTextSwitchReport and keeps the `Open Rules Window...` row last.
    - MenuBarViewModel translates aggregate report counts into user-facing success and partial-failure copy defined in the UI-SPEC.
    - DefaultEditorSwitcherApp wires the rules-window scene ID used by the menu footer action.
---

<objective>
Add post-apply feedback, refresh affordances, and the rules-window entry point that completes the Phase 2 utility experience.

Purpose: Phase 2 is only useful if users can trust the result immediately after a switch and still reach a fuller window experience without sacrificing the menu-bar-first interaction model.
Output: In-menu success and failure messaging, inline current-editor updates, an explicit rules-window footer action, and tests for feedback and secondary-window entry points.
</objective>

<execution_context>
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-menu-bar-global-switch/02-CONTEXT.md
@.planning/phases/02-menu-bar-global-switch/02-RESEARCH.md
@.planning/phases/02-menu-bar-global-switch/02-UI-SPEC.md
@.planning/phases/02-menu-bar-global-switch/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render in-menu feedback and explicit current-editor markers</name>
  <files>App/Features/MenuBar/MenuBarContentView.swift, App/Features/MenuBar/MenuBarViewModel.swift</files>
  <read_first>App/Features/MenuBar/MenuBarContentView.swift, App/Features/MenuBar/MenuBarViewModel.swift, App/Application/GlobalText/GlobalTextSwitchReport.swift, .planning/phases/02-menu-bar-global-switch/02-UI-SPEC.md</read_first>
  <action>Update `App/Features/MenuBar/MenuBarViewModel.swift` to translate `GlobalTextSwitchReport` into explicit user-facing feedback strings using the UI contract: success copy must read `All text files now open in {Editor}` and partial-failure copy must start with `Some file types stayed on another app`. Update `App/Features/MenuBar/MenuBarContentView.swift` so the feedback row sits directly below the current-state summary, the currently effective editor row shows a checkmark plus `Current`, and lower-confidence rows include a capability note such as `Partial support` or `Needs verification` instead of looking identical to primary actions.</action>
  <verify>rg -n "All text files now open in|Some file types stayed on another app|Current|Partial support|Needs verification" App/Features/MenuBar/MenuBarContentView.swift App/Features/MenuBar/MenuBarViewModel.swift</verify>
  <acceptance_criteria>
    - App/Features/MenuBar/MenuBarViewModel.swift contains "All text files now open in"
    - App/Features/MenuBar/MenuBarViewModel.swift contains "Some file types stayed on another app"
    - App/Features/MenuBar/MenuBarContentView.swift contains "Current"
    - App/Features/MenuBar/MenuBarContentView.swift renders capability notes for lower-confidence rows
  </acceptance_criteria>
  <done>The menu now explains the outcome of the latest switch attempt without leaving the menu flow.</done>
</task>

<task type="auto">
  <name>Task 2: Add the rules-window placeholder and window-opening action</name>
  <files>App/DefaultEditorSwitcherApp.swift, App/Features/MenuBar/RulesWindowPlaceholderView.swift, App/Features/MenuBar/MenuBarContentView.swift</files>
  <read_first>App/DefaultEditorSwitcherApp.swift, App/Features/MenuBar/MenuBarContentView.swift, .planning/phases/02-menu-bar-global-switch/02-CONTEXT.md, .planning/phases/02-menu-bar-global-switch/02-UI-SPEC.md</read_first>
  <action>Create `App/Features/MenuBar/RulesWindowPlaceholderView.swift` with explicit placeholder copy that says advanced language and custom extension rules arrive in later phases. Ensure `App/DefaultEditorSwitcherApp.swift` attaches that view to the `rules-window` scene ID created in Plan 01. Update `App/Features/MenuBar/MenuBarContentView.swift` so the final footer action uses `openWindow(id: "rules-window")` or the equivalent app-shell hook and is always rendered after a separator with the exact label `Open Rules Window...`.</action>
  <verify>rg -n "rules-window|Open Rules Window|later phases|advanced language" App/DefaultEditorSwitcherApp.swift App/Features/MenuBar/RulesWindowPlaceholderView.swift App/Features/MenuBar/MenuBarContentView.swift</verify>
  <acceptance_criteria>
    - App/Features/MenuBar/RulesWindowPlaceholderView.swift exists
    - RulesWindowPlaceholderView.swift contains copy explaining that advanced rules arrive in later phases
    - App/Features/MenuBar/MenuBarContentView.swift contains `Open Rules Window...`
    - App/Features/MenuBar/MenuBarContentView.swift opens `rules-window`
  </acceptance_criteria>
  <done>The resident menu utility now has a real secondary-window escape hatch for advanced configuration.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for feedback text and rules-window exposure</name>
  <files>Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift</files>
  <read_first>App/Features/MenuBar/MenuBarViewModel.swift, App/Features/MenuBar/RulesWindowPlaceholderView.swift, .planning/phases/02-menu-bar-global-switch/02-VALIDATION.md</read_first>
  <action>Extend `Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift` with tests named `testApplyPublishesSuccessOrFailureFeedback()` and `testOpenRulesWindowActionIsExposed()`. The first test must assert the exact success prefix `All text files now open in` and partial-failure prefix `Some file types stayed on another app`. The second test must assert that the menu model or view-facing actions include an `Open Rules Window...` footer action targeting the `rules-window` scene ID.</action>
  <verify>rg -n "testApplyPublishesSuccessOrFailureFeedback|testOpenRulesWindowActionIsExposed|All text files now open in|Some file types stayed on another app|rules-window" Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift</verify>
  <acceptance_criteria>
    - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift contains "testApplyPublishesSuccessOrFailureFeedback"
    - MenuBarViewModelTests.swift contains the exact strings "All text files now open in" and "Some file types stayed on another app"
    - MenuBarViewModelTests.swift contains "testOpenRulesWindowActionIsExposed"
    - MenuBarViewModelTests.swift contains "rules-window"
  </acceptance_criteria>
  <done>Feedback and advanced-window handoff behavior are guarded by tests rather than informal manual inspection only.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "All text files now open in|Some file types stayed on another app|Open Rules Window" App/Features/MenuBar/MenuBarContentView.swift App/Features/MenuBar/MenuBarViewModel.swift` succeeds
- [ ] `rg -n "rules-window" App/DefaultEditorSwitcherApp.swift App/Features/MenuBar/RulesWindowPlaceholderView.swift` succeeds
- [ ] `xcodebuild test -scheme DefaultEditorSwitcher -destination 'platform=macOS' -only-testing:DefaultEditorSwitcherTests/MenuBarViewModelTests` succeeds
</verification>

<success_criteria>

- All tasks completed
- The menu surfaces success or partial-failure feedback without opening another window
- Users can refresh trust in the current editor directly from the menu and see the active editor inline
- The app remains a menu bar utility while still opening a separate rules-window placeholder
  </success_criteria>

<output>
After completion, create `.planning/phases/02-menu-bar-global-switch/02-03-SUMMARY.md`
</output>

---
phase: 02-menu-bar-global-switch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DefaultEditorSwitcher.xcodeproj/project.pbxproj
  - App/DefaultEditorSwitcherApp.swift
  - App/Features/MenuBar/MenuBarContentView.swift
  - App/Features/MenuBar/MenuBarViewModel.swift
  - App/Features/MenuBar/MenuBarSection.swift
  - App/Application/GlobalText/GlobalTextStateService.swift
  - Tests/DefaultEditorSwitcherTests/GlobalTextStateServiceTests.swift
  - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift
autonomous: true
requirements:
  - MENU-01
  - MENU-02
user_setup: []
must_haves:
  truths:
    - The app launches as a resident menu bar utility with a visible `MenuBarExtra` entry.
    - Opening the menu shows a current global editor summary derived from the full developer-text scope, not a single sample extension.
    - The menu lists recommended full-support editors before lower-priority eligible editors without mutating defaults yet.
  artifacts:
    - App/DefaultEditorSwitcherApp.swift
    - App/Features/MenuBar/MenuBarContentView.swift
    - App/Features/MenuBar/MenuBarViewModel.swift
    - App/Application/GlobalText/GlobalTextStateService.swift
    - Tests/DefaultEditorSwitcherTests/GlobalTextStateServiceTests.swift
    - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift
  key_links:
    - GlobalTextStateService expands `FileScope.allText` via `ContentTypeResolver` and reads current bundle IDs through `LaunchServicesClient`.
    - MenuBarViewModel consumes GlobalTextStateService and WorkspaceAppDiscovery to build the summary row and candidate sections.
    - MenuBarContentView renders the ordered sections defined by the UI-SPEC contract.
---

<objective>
Build the resident app shell, current-state summary, and editor-list menu UI for the Phase 2 quick-switch flow.

Purpose: The product promise is a one-step menu bar utility, so the app needs a real resident shell and a truthful read-only view of the current global text editor before apply actions are wired.
Output: A `MenuBarExtra` app shell, a state service that summarizes the current global text binding, grouped menu sections for full versus lower-confidence editors, and tests for summary/candidate ordering.
</objective>

<execution_context>
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-menu-bar-global-switch/02-CONTEXT.md
@.planning/phases/02-menu-bar-global-switch/02-RESEARCH.md
@.planning/phases/02-menu-bar-global-switch/02-UI-SPEC.md
@.planning/phases/02-menu-bar-global-switch/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the app entry into a resident MenuBarExtra shell</name>
  <files>DefaultEditorSwitcher.xcodeproj/project.pbxproj, App/DefaultEditorSwitcherApp.swift, App/Features/MenuBar/MenuBarContentView.swift</files>
  <read_first>App/DefaultEditorSwitcherApp.swift, .planning/phases/02-menu-bar-global-switch/02-CONTEXT.md, .planning/phases/02-menu-bar-global-switch/02-UI-SPEC.md, .planning/phases/02-menu-bar-global-switch/02-RESEARCH.md</read_first>
  <action>Update `App/DefaultEditorSwitcherApp.swift` so the app's primary scene is a `MenuBarExtra("Default Editor Switcher", systemImage: "slider.horizontal.3")` or equivalent symbol-based entry instead of a bare `Settings` scene. Add a `WindowGroup` with a stable ID such as `rules-window` and a lightweight placeholder title, but do not expose advanced rule-editing controls yet. Create `App/Features/MenuBar/MenuBarContentView.swift` to render the menu in the exact order locked by `02-UI-SPEC.md`: current-state summary first, then optional feedback, then the editor sections, then a separator and `Open Rules Window...` as the last row. Ensure every action row has a text label and no icon-only controls.</action>
  <verify>rg -n "MenuBarExtra|WindowGroup|rules-window|Open Rules Window" App/DefaultEditorSwitcherApp.swift App/Features/MenuBar/MenuBarContentView.swift DefaultEditorSwitcher.xcodeproj/project.pbxproj</verify>
  <acceptance_criteria>
    - App/DefaultEditorSwitcherApp.swift contains "MenuBarExtra"
    - App/DefaultEditorSwitcherApp.swift contains "WindowGroup" and "rules-window"
    - App/Features/MenuBar/MenuBarContentView.swift contains "Open Rules Window"
    - App/Features/MenuBar/MenuBarContentView.swift contains a current-state summary label before editor sections
  </acceptance_criteria>
  <done>The app now behaves like a real menu bar utility with a future rules-window hook.</done>
</task>

<task type="auto">
  <name>Task 2: Implement a global-text state service and menu section model</name>
  <files>App/Application/GlobalText/GlobalTextStateService.swift, App/Features/MenuBar/MenuBarSection.swift, App/Features/MenuBar/MenuBarViewModel.swift</files>
  <read_first>App/Domain/Types/ContentTypeResolver.swift, App/Infrastructure/LaunchServices/LaunchServicesClient.swift, App/Infrastructure/Workspace/WorkspaceAppDiscovery.swift, App/Support/KnownEditors.swift, .planning/phases/02-menu-bar-global-switch/02-RESEARCH.md</read_first>
  <action>Create `App/Application/GlobalText/GlobalTextStateService.swift` with a public API that reads `ContentTypeResolver.resolutions(for: .allText)`, filters to declared `UTType` values, and summarizes the current editor state as either a single effective bundle ID, a mixed state with distinct bundle IDs, or unavailable. Add `App/Features/MenuBar/MenuBarSection.swift` with concrete section cases or models for `recommendedFullSupport`, `otherEligible`, and `needsVerification`, including row labels and capability notes. Update `App/Features/MenuBar/MenuBarViewModel.swift` so `load()` fetches the current global-text summary from `GlobalTextStateService`, discovers ranked editor candidates from `WorkspaceAppDiscovery` using `UTType.plainText`, and partitions candidates so `.full` rows appear before `.partial` and `.unverified` rows.</action>
  <verify>rg -n "resolutions\\(for: \\.allText\\)|single|mixed|unavailable|recommendedFullSupport|needsVerification|UTType\\.plainText" App/Application/GlobalText/GlobalTextStateService.swift App/Features/MenuBar/MenuBarSection.swift App/Features/MenuBar/MenuBarViewModel.swift</verify>
  <acceptance_criteria>
    - App/Application/GlobalText/GlobalTextStateService.swift contains "resolutions(for: .allText)"
    - App/Application/GlobalText/GlobalTextStateService.swift contains states for "single", "mixed", and "unavailable" or equivalent cases
    - App/Features/MenuBar/MenuBarViewModel.swift contains "UTType.plainText"
    - App/Features/MenuBar/MenuBarViewModel.swift groups full-support candidates ahead of partial or unverified candidates
  </acceptance_criteria>
  <done>The menu has a truthful read model for current state and an ordered candidate list ready for apply actions.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for current-state aggregation and menu loading</name>
  <files>Tests/DefaultEditorSwitcherTests/GlobalTextStateServiceTests.swift, Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift</files>
  <read_first>App/Application/GlobalText/GlobalTextStateService.swift, App/Features/MenuBar/MenuBarViewModel.swift, App/Infrastructure/Workspace/WorkspaceAppDiscovery.swift, .planning/phases/02-menu-bar-global-switch/02-VALIDATION.md</read_first>
  <action>Create `Tests/DefaultEditorSwitcherTests/GlobalTextStateServiceTests.swift` with cases that prove the service returns unified state when all declared types share one bundle ID, mixed state when at least two different effective bundle IDs are present, and unavailable when no declared type has a current handler. Create `Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift` with tests named `testLoadsCurrentStateAndCandidateSections()` and `testRecommendedFullSupportCandidatesAppearBeforeLowerConfidenceSections()` using injected fake services so the menu model is verified without AppKit UI rendering.</action>
  <verify>rg -n "testLoadsCurrentStateAndCandidateSections|testRecommendedFullSupportCandidatesAppearBeforeLowerConfidenceSections|mixed|unavailable" Tests/DefaultEditorSwitcherTests/GlobalTextStateServiceTests.swift Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift</verify>
  <acceptance_criteria>
    - Tests/DefaultEditorSwitcherTests/GlobalTextStateServiceTests.swift exists
    - GlobalTextStateServiceTests.swift contains a mixed-state test and an unavailable-state test
    - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift contains "testLoadsCurrentStateAndCandidateSections"
    - MenuBarViewModelTests.swift contains "testRecommendedFullSupportCandidatesAppearBeforeLowerConfidenceSections"
  </acceptance_criteria>
  <done>The read-only menu shell is protected by repeatable unit tests before mutation logic is introduced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "MenuBarExtra|rules-window" App/DefaultEditorSwitcherApp.swift` succeeds
- [ ] `rg -n "resolutions\\(for: \\.allText\\)|UTType\\.plainText" App/Application/GlobalText/GlobalTextStateService.swift App/Features/MenuBar/MenuBarViewModel.swift` succeeds
- [ ] `xcodebuild test -scheme DefaultEditorSwitcher -destination 'platform=macOS' -only-testing:DefaultEditorSwitcherTests/GlobalTextStateServiceTests -only-testing:DefaultEditorSwitcherTests/MenuBarViewModelTests` succeeds
</verification>

<success_criteria>

- All tasks completed
- The app has a resident `MenuBarExtra` shell
- The current-state summary reflects the full developer-text scope instead of a single sample type
- The menu clearly separates full-support editor actions from lower-confidence options
  </success_criteria>

<output>
After completion, create `.planning/phases/02-menu-bar-global-switch/02-01-SUMMARY.md`
</output>

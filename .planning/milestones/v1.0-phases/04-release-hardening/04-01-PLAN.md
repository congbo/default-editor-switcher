---
phase: 04-release-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DefaultEditorSwitcher.xcodeproj/project.pbxproj
  - Tools/Release/export-options.plist
  - Tools/Release/build-release.sh
  - Tools/Release/verify-artifact.sh
  - Tools/Release/README.md
autonomous: true
requirements:
  - DIST-01
user_setup:
  - Install a valid `Developer ID Application` certificate in the login keychain used for release builds.
  - Store notarization credentials with `xcrun notarytool store-credentials <profile-name>` before running the release script.
must_haves:
  truths:
    - The app target can produce a signed Release archive with hardened runtime enabled instead of remaining effectively unsigned.
    - A single repo-owned script builds, exports, zips, notarizes, staples, and records a direct-download release artifact for `DefaultEditorSwitcher.app`.
    - Artifact verification is scriptable and proves signature integrity, notarization acceptance, and Gatekeeper acceptance before install.
  artifacts:
    - DefaultEditorSwitcher.xcodeproj/project.pbxproj
    - Tools/Release/export-options.plist
    - Tools/Release/build-release.sh
    - Tools/Release/verify-artifact.sh
    - Tools/Release/README.md
  key_links:
    - The app target Release configuration must enable signing and hardened runtime while test and probe targets remain unsigned.
    - `build-release.sh` is the single operator entrypoint and leaves outputs under `build/release/`.
    - `verify-artifact.sh` checks the exported app and final zip with `codesign`, `spctl`, `stapler`, and notarization evidence.
---

<objective>
Build the release pipeline for signing, notarization, and pre-install artifact verification.

Purpose: Phase 4 needs a repeatable direct-download release path before the final UX hardening and installed-app validation can be trusted.
Output: Release-capable Xcode settings, a notarized zip build script, an artifact verification script, and operator docs describing the exact release contract.
</objective>

<execution_context>
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-hardening/04-CONTEXT.md
@.planning/phases/04-release-hardening/04-RESEARCH.md
@.planning/phases/04-release-hardening/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable release signing and export settings for the app target</name>
  <files>DefaultEditorSwitcher.xcodeproj/project.pbxproj, Tools/Release/export-options.plist</files>
  <read_first>DefaultEditorSwitcher.xcodeproj/project.pbxproj, .planning/phases/04-release-hardening/04-RESEARCH.md, .planning/phases/04-release-hardening/04-VALIDATION.md</read_first>
  <action>Update `DefaultEditorSwitcher.xcodeproj/project.pbxproj` so the `DefaultEditorSwitcher` app target Release configuration uses `CODE_SIGNING_ALLOWED = YES`, `CODE_SIGNING_REQUIRED = YES`, `CODE_SIGN_STYLE = Manual`, and `ENABLE_HARDENED_RUNTIME = YES`, while leaving the test target and `AssociationProbe` Release configs unsigned. Add `Tools/Release/export-options.plist` with a concrete macOS app-export configuration for direct distribution: `method = developer-id`, `destination = export`, `signingStyle = manual`, `stripSwiftSymbols = true`, and `teamID = $(DEVELOPMENT_TEAM_ID)` if the plist supports expansion, otherwise document that `xcodebuild -exportArchive` receives `DEVELOPMENT_TEAM_ID` from the environment.</action>
  <verify>rg -n "CODE_SIGNING_ALLOWED = YES|CODE_SIGNING_REQUIRED = YES|ENABLE_HARDENED_RUNTIME = YES" DefaultEditorSwitcher.xcodeproj/project.pbxproj && plutil -lint Tools/Release/export-options.plist</verify>
  <acceptance_criteria>
    - DefaultEditorSwitcher.xcodeproj/project.pbxproj contains `CODE_SIGNING_ALLOWED = YES` in the app target Release configuration
    - DefaultEditorSwitcher.xcodeproj/project.pbxproj contains `ENABLE_HARDENED_RUNTIME = YES`
    - Tools/Release/export-options.plist exists
    - export-options.plist contains `developer-id`
  </acceptance_criteria>
  <done>The repository can archive and export the shipping app target with Developer ID signing instead of treating Release as unsigned-only.</done>
</task>

<task type="auto">
  <name>Task 2: Create a single release script for archive, export, zip, notarize, and staple</name>
  <files>Tools/Release/build-release.sh</files>
  <read_first>DefaultEditorSwitcher.xcodeproj/project.pbxproj, Tools/Release/export-options.plist, .planning/phases/04-release-hardening/04-RESEARCH.md</read_first>
  <action>Create `Tools/Release/build-release.sh` as the operator entrypoint. The script must require these exact environment variables before doing any work: `DEVELOPMENT_TEAM_ID`, `CODE_SIGN_IDENTITY`, and `NOTARY_PROFILE`. It must build into `build/release/` using these exact stages in order: 1) `xcodebuild archive -scheme DefaultEditorSwitcher -configuration Release -destination 'generic/platform=macOS' -archivePath build/release/DefaultEditorSwitcher.xcarchive DEVELOPMENT_TEAM=\"$DEVELOPMENT_TEAM_ID\" CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY=\"$CODE_SIGN_IDENTITY\" ENABLE_HARDENED_RUNTIME=YES`; 2) `xcodebuild -exportArchive` into `build/release/exported` using `Tools/Release/export-options.plist`; 3) `ditto -c -k --keepParent` to create `build/release/DefaultEditorSwitcher-macOS.zip`; 4) `xcrun notarytool submit --wait` against that zip using `--keychain-profile \"$NOTARY_PROFILE\"`; 5) `xcrun stapler staple` against the exported `.app`; 6) re-zip the stapled app and write a small manifest file such as `build/release/release-manifest.txt` that records archive path, zip path, and notarization completion.</action>
  <verify>bash -n Tools/Release/build-release.sh && rg -n "DEVELOPMENT_TEAM_ID|CODE_SIGN_IDENTITY|NOTARY_PROFILE|xcodebuild archive|xcodebuild -exportArchive|notarytool submit|stapler staple|ditto -c -k" Tools/Release/build-release.sh</verify>
  <acceptance_criteria>
    - Tools/Release/build-release.sh exists
    - build-release.sh checks `DEVELOPMENT_TEAM_ID`, `CODE_SIGN_IDENTITY`, and `NOTARY_PROFILE`
    - build-release.sh contains `xcodebuild archive`
    - build-release.sh contains `notarytool submit`
    - build-release.sh writes into `build/release/`
  </acceptance_criteria>
  <done>The repo has a reproducible local release command instead of a memory-based manual Organizer flow.</done>
</task>

<task type="auto">
  <name>Task 3: Add pre-install artifact verification and release operator documentation</name>
  <files>Tools/Release/verify-artifact.sh, Tools/Release/README.md</files>
  <read_first>Tools/Release/build-release.sh, .planning/phases/04-release-hardening/04-RESEARCH.md, .planning/phases/04-release-hardening/04-VALIDATION.md</read_first>
  <action>Create `Tools/Release/verify-artifact.sh` that accepts two concrete arguments: the exported app path and the final zip path. The script must run `codesign --verify --deep --strict --verbose=2 "$APP_PATH"`, `spctl -a -vv --type execute "$APP_PATH"`, `xcrun stapler validate "$APP_PATH"`, and a notarization evidence command such as `xcrun notarytool log` when a submission ID is present in `build/release/release-manifest.txt`; it must exit non-zero on the first failed check. Create `Tools/Release/README.md` documenting the exact environment variables, the `./Tools/Release/build-release.sh` command, the `./Tools/Release/verify-artifact.sh build/release/exported/DefaultEditorSwitcher.app build/release/DefaultEditorSwitcher-macOS.zip` command, and the expected outputs under `build/release/`.</action>
  <verify>bash -n Tools/Release/verify-artifact.sh && rg -n "codesign --verify|spctl -a -vv|stapler validate|notarytool log|build-release.sh|verify-artifact.sh|build/release/" Tools/Release/verify-artifact.sh Tools/Release/README.md</verify>
  <acceptance_criteria>
    - Tools/Release/verify-artifact.sh exists
    - verify-artifact.sh contains `codesign --verify --deep --strict --verbose=2`
    - verify-artifact.sh contains `spctl -a -vv --type execute`
    - Tools/Release/README.md contains `DEVELOPMENT_TEAM_ID`
    - Tools/Release/README.md contains `build/release/`
  </acceptance_criteria>
  <done>The release pipeline leaves behind a scriptable evidence trail for signing and notarization before the app is installed anywhere.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "CODE_SIGNING_ALLOWED = YES|ENABLE_HARDENED_RUNTIME = YES" DefaultEditorSwitcher.xcodeproj/project.pbxproj` succeeds
- [ ] `plutil -lint Tools/Release/export-options.plist` succeeds
- [ ] `bash -n Tools/Release/build-release.sh Tools/Release/verify-artifact.sh` succeeds
- [ ] `rg -n "notarytool submit|stapler staple|spctl -a -vv|codesign --verify" Tools/Release/build-release.sh Tools/Release/verify-artifact.sh` succeeds
</verification>

<success_criteria>

- All tasks completed
- The repo can produce a signed Release archive and notarized zip from a documented single command
- Artifact verification is scriptable before installation
- Release operators no longer need ad hoc shell history to ship the app
  </success_criteria>

<output>
After completion, create `.planning/phases/04-release-hardening/04-01-SUMMARY.md`
</output>

---
phase: 04-release-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - App/Application/GlobalText/GlobalTextSwitchReport.swift
  - App/Features/MenuBar/GlobalTextSwitchFeedbackFormatter.swift
  - App/Features/MenuBar/MenuBarViewModel.swift
  - App/Features/MenuBar/MenuBarContentView.swift
  - App/Resources/Localizable.xcstrings
  - Tests/DefaultEditorSwitcherTests/GlobalTextSwitchCoordinatorTests.swift
  - Tests/DefaultEditorSwitcherTests/GlobalTextSwitchFeedbackFormatterTests.swift
  - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift
  - Tools/Release/verify-installed-app.sh
  - Tools/Release/clean-machine-checklist.md
autonomous: true
requirements:
  - DIST-01
  - DIST-02
user_setup: []
must_haves:
  truths:
    - Partial or failed association writes surface a localized headline, affected-scope details, and an explicit recovery action instead of remaining internal-only report data.
    - The menu-bar flow stays small and fast, but the user can open `Settings...` directly from the recovery affordance.
    - Installed-artifact validation proves both packaging trust and core product behavior outside Xcode.
  artifacts:
    - App/Application/GlobalText/GlobalTextSwitchReport.swift
    - App/Features/MenuBar/GlobalTextSwitchFeedbackFormatter.swift
    - App/Features/MenuBar/MenuBarViewModel.swift
    - App/Features/MenuBar/MenuBarContentView.swift
    - App/Resources/Localizable.xcstrings
    - Tests/DefaultEditorSwitcherTests/GlobalTextSwitchFeedbackFormatterTests.swift
    - Tools/Release/verify-installed-app.sh
    - Tools/Release/clean-machine-checklist.md
  key_links:
    - `GlobalTextSwitchReport` remains the truth source; feedback formatting derives from it instead of inventing a second status model.
    - `MenuBarViewModel` owns the published recovery state and clears it after a later full-success switch.
    - Installed-artifact verification uses the signed output from Plan 01 and validates launch plus one success/failure product path.
---

<objective>
Harden failure UX, localized recovery messaging, and installed-artifact release checks.

Purpose: The shipped app must not only pass Gatekeeper, but also tell the user exactly what went wrong when Launch Services updates fail fully or partially.
Output: Scope-aware localized feedback in the menu bar flow, targeted regression tests, and an installed-app verification script plus checklist for release candidates.
</objective>

<execution_context>
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-hardening/04-CONTEXT.md
@.planning/phases/04-release-hardening/04-RESEARCH.md
@.planning/phases/04-release-hardening/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend global-switch failure data and add a localized feedback formatter</name>
  <files>App/Application/GlobalText/GlobalTextSwitchReport.swift, App/Features/MenuBar/GlobalTextSwitchFeedbackFormatter.swift, App/Resources/Localizable.xcstrings, Tests/DefaultEditorSwitcherTests/GlobalTextSwitchCoordinatorTests.swift, Tests/DefaultEditorSwitcherTests/GlobalTextSwitchFeedbackFormatterTests.swift</files>
  <read_first>App/Application/GlobalText/GlobalTextSwitchReport.swift, App/Application/GlobalText/GlobalTextSwitchCoordinator.swift, App/Domain/Associations/AssociationVerificationResult.swift, App/Application/Localization/AppLanguageStore.swift, .planning/phases/04-release-hardening/04-RESEARCH.md</read_first>
  <action>Extend `App/Application/GlobalText/GlobalTextSwitchReport.swift` so each `SampleFailure` can describe the failed scope in user-facing terms. Add fields that carry a concrete `scopeLabel` and keep the existing `status`, `effectiveBundleID`, and `statusCode`. Update `GlobalTextSwitchCoordinator` usage as needed so `scopeLabel` prefers a filename-extension example such as `.md` or `.py` when one is known for the failing content type, and otherwise falls back to the UTType identifier. Create `App/Features/MenuBar/GlobalTextSwitchFeedbackFormatter.swift` that converts a `GlobalTextSwitchReport` into a localized presentation model with these exact outputs: a headline using `%d text types could not switch to %@.`, up to three detail lines that include the scope label plus one of `Still opens in %@`, `This editor does not support this type on this Mac`, or `macOS rejected the change (OSStatus %d)`, and a recovery action title `Open Settings for Recovery`. Add the new English and Simplified Chinese strings to `App/Resources/Localizable.xcstrings`. Add `GlobalTextSwitchFeedbackFormatterTests.swift` and update `GlobalTextSwitchCoordinatorTests.swift` to prove `scopeLabel` and status-specific detail data are populated.</action>
  <verify>rg -n "scopeLabel|Open Settings for Recovery|Still opens in %@|This editor does not support this type on this Mac|macOS rejected the change" App/Application/GlobalText/GlobalTextSwitchReport.swift App/Features/MenuBar/GlobalTextSwitchFeedbackFormatter.swift App/Resources/Localizable.xcstrings && xcodebuild test -scheme DefaultEditorSwitcher -destination 'platform=macOS' -only-testing:DefaultEditorSwitcherTests/GlobalTextSwitchCoordinatorTests -only-testing:DefaultEditorSwitcherTests/GlobalTextSwitchFeedbackFormatterTests</verify>
  <acceptance_criteria>
    - App/Application/GlobalText/GlobalTextSwitchReport.swift contains `scopeLabel`
    - App/Features/MenuBar/GlobalTextSwitchFeedbackFormatter.swift exists
    - Localizable.xcstrings contains `Open Settings for Recovery`
    - GlobalTextSwitchFeedbackFormatterTests.swift exists
    - GlobalTextSwitchCoordinatorTests prove sample failures include scope labels
  </acceptance_criteria>
  <done>The app has a concrete, localizable way to explain partial and failed switch results using the existing verification truth source.</done>
</task>

<task type="auto">
  <name>Task 2: Surface recovery feedback in the menu bar flow and clear stale warnings after success</name>
  <files>App/Features/MenuBar/MenuBarViewModel.swift, App/Features/MenuBar/MenuBarContentView.swift, Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift</files>
  <read_first>App/Features/MenuBar/MenuBarViewModel.swift, App/Features/MenuBar/MenuBarContentView.swift, App/Features/MenuBar/MenuBarSection.swift, App/Features/MenuBar/GlobalTextSwitchFeedbackFormatter.swift, Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift</read_first>
  <action>Update `MenuBarViewModel` to own a published feedback property such as `lastSwitchFeedback` that is populated only when the latest `GlobalTextSwitchReport` has `affectedCount > 0` and cleared when a later report `didFullyMatch == true`. Keep `lastSwitchReport` for diagnostics and tests, but replace the current no-feedback behavior with an explicit recovery presentation. Update `MenuBarContentView` so that when feedback exists it renders, above the `More` submenu trigger, one disabled headline row, up to three disabled detail rows, and a button labeled `Open Settings for Recovery` that activates the app and opens `settings-window`. Do not hide the normal editor rows; the feedback block must augment the fast-switch flow, not replace it. Update `MenuBarViewModelTests.swift` by replacing the current assertion that failures do not publish menu feedback with tests that assert failed applies publish recovery copy and later successful applies clear it.</action>
  <verify>rg -n "lastSwitchFeedback|Open Settings for Recovery|settings-window" App/Features/MenuBar/MenuBarViewModel.swift App/Features/MenuBar/MenuBarContentView.swift Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift && xcodebuild test -scheme DefaultEditorSwitcher -destination 'platform=macOS' -only-testing:DefaultEditorSwitcherTests/MenuBarViewModelTests</verify>
  <acceptance_criteria>
    - App/Features/MenuBar/MenuBarViewModel.swift contains `lastSwitchFeedback`
    - App/Features/MenuBar/MenuBarContentView.swift contains `Open Settings for Recovery`
    - MenuBarViewModelTests assert failed applies publish recovery feedback
    - MenuBarViewModelTests assert a later full-success apply clears stale feedback
  </acceptance_criteria>
  <done>The menu bar explains actionable failures without losing the app's one-step switching interaction.</done>
</task>

<task type="auto">
  <name>Task 3: Add installed-artifact verification and a clean-machine release checklist</name>
  <files>Tools/Release/verify-installed-app.sh, Tools/Release/clean-machine-checklist.md</files>
  <read_first>Tools/Release/build-release.sh, Tools/Release/verify-artifact.sh, Tools/Release/README.md, .planning/phases/04-release-hardening/04-VALIDATION.md</read_first>
  <action>Create `Tools/Release/verify-installed-app.sh` that accepts one concrete argument, the installed app path. The script must run `codesign --verify --deep --strict --verbose=2 "$APP_PATH"`, `spctl -a -vv --type execute "$APP_PATH"`, `open -na "$APP_PATH"`, wait briefly, assert the app is running with `pgrep -x DefaultEditorSwitcher`, then print the next manual product-check instructions before exiting. Add `Tools/Release/clean-machine-checklist.md` with an ordered release-candidate checklist that includes: run `./Tools/Release/build-release.sh`, run `./Tools/Release/verify-artifact.sh`, move the app into `/Applications`, run `./Tools/Release/verify-installed-app.sh /Applications/DefaultEditorSwitcher.app`, confirm one successful editor switch, and confirm one failure scenario shows the recovery block plus `Open Settings for Recovery`.</action>
  <verify>bash -n Tools/Release/verify-installed-app.sh && rg -n "open -na|pgrep -x DefaultEditorSwitcher|spctl -a -vv|codesign --verify|Open Settings for Recovery" Tools/Release/verify-installed-app.sh Tools/Release/clean-machine-checklist.md</verify>
  <acceptance_criteria>
    - Tools/Release/verify-installed-app.sh exists
    - verify-installed-app.sh contains `open -na`
    - verify-installed-app.sh contains `pgrep -x DefaultEditorSwitcher`
    - Tools/Release/clean-machine-checklist.md contains `verify-installed-app.sh /Applications/DefaultEditorSwitcher.app`
    - clean-machine-checklist.md contains `Open Settings for Recovery`
  </acceptance_criteria>
  <done>The release candidate has a concrete post-install validation path that proves both trust signals and user-visible recovery behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild test -scheme DefaultEditorSwitcher -destination 'platform=macOS' -only-testing:DefaultEditorSwitcherTests/GlobalTextSwitchCoordinatorTests -only-testing:DefaultEditorSwitcherTests/GlobalTextSwitchFeedbackFormatterTests -only-testing:DefaultEditorSwitcherTests/MenuBarViewModelTests` succeeds
- [ ] `rg -n "Open Settings for Recovery|Still opens in %@|This editor does not support this type on this Mac|macOS rejected the change" App/Features/MenuBar/GlobalTextSwitchFeedbackFormatter.swift App/Resources/Localizable.xcstrings` succeeds
- [ ] `bash -n Tools/Release/verify-installed-app.sh` succeeds
- [ ] `rg -n "open -na|pgrep -x DefaultEditorSwitcher" Tools/Release/verify-installed-app.sh Tools/Release/clean-machine-checklist.md` succeeds
</verification>

<success_criteria>

- All tasks completed
- Partial or failed global switches show scope-specific recovery guidance in the menu bar flow
- Recovery guidance opens the existing Settings window instead of a new complex flow
- Release candidates have an installed-app verification script and checklist outside Xcode
  </success_criteria>

<output>
After completion, create `.planning/phases/04-release-hardening/04-02-SUMMARY.md`
</output>

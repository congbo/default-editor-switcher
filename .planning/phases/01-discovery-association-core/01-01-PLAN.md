---
phase: 01-discovery-association-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DefaultEditorSwitcher.xcodeproj/project.pbxproj
  - App/DefaultEditorSwitcherApp.swift
  - App/Domain/Types/FileScope.swift
  - App/Domain/Types/LanguageBucket.swift
  - App/Domain/Types/ContentTypeResolver.swift
  - Tests/DefaultEditorSwitcherTests/FileScopeCatalogTests.swift
autonomous: true
requirements:
  - GLOB-02
user_setup: []
must_haves:
  truths:
    - The codebase has a macOS app scaffold and test target that can compile under the DefaultEditorSwitcher scheme.
    - The developer-strict global text scope resolves into explicit extensions and UTType-backed metadata.
    - Source-code buckets are modeled as members of the global text baseline rather than a separate top-level scope.
  artifacts:
    - DefaultEditorSwitcher.xcodeproj/project.pbxproj
    - App/Domain/Types/FileScope.swift
    - App/Domain/Types/LanguageBucket.swift
    - App/Domain/Types/ContentTypeResolver.swift
    - Tests/DefaultEditorSwitcherTests/FileScopeCatalogTests.swift
  key_links:
    - FileScope and LanguageBucket definitions are consumed by ContentTypeResolver.
    - FileScopeCatalogTests assert the same extension sets and type-conformance rules encoded in the domain layer.
---

<objective>
Create the initial macOS app/test scaffold and define the phase's source-of-truth taxonomy for developer-strict text scopes.

Purpose: Every later discovery and association service depends on a stable project skeleton and a shared file-scope model.
Output: A buildable macOS project, file-scope domain models, UTType resolver utilities, and unit tests for the global text baseline.
</objective>

<execution_context>
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-discovery-association-core/01-CONTEXT.md
@.planning/phases/01-discovery-association-core/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap the macOS app and test target scaffold</name>
  <files>DefaultEditorSwitcher.xcodeproj/project.pbxproj, App/DefaultEditorSwitcherApp.swift</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-discovery-association-core/01-CONTEXT.md, .planning/phases/01-discovery-association-core/01-RESEARCH.md</read_first>
  <action>Create an Xcode macOS app project named `DefaultEditorSwitcher` with one app target and one unit-test target named `DefaultEditorSwitcherTests`. Place the app entry file at `App/DefaultEditorSwitcherApp.swift`. In that file, declare `@main struct DefaultEditorSwitcherApp: App` and render a minimal `Settings { EmptyView() }` or equivalent no-op shell so the target builds even before UI phases begin. Keep deployment target at macOS 14 or newer and ensure the project file contains a scheme/build configuration for `DefaultEditorSwitcher` and the `DefaultEditorSwitcherTests` target.</action>
  <verify>test -f DefaultEditorSwitcher.xcodeproj/project.pbxproj && rg -n "DefaultEditorSwitcherTests|DefaultEditorSwitcherApp" DefaultEditorSwitcher.xcodeproj/project.pbxproj App/DefaultEditorSwitcherApp.swift</verify>
  <acceptance_criteria>
    - DefaultEditorSwitcher.xcodeproj/project.pbxproj exists
    - App/DefaultEditorSwitcherApp.swift contains "@main" and "struct DefaultEditorSwitcherApp: App"
    - DefaultEditorSwitcher.xcodeproj/project.pbxproj contains "DefaultEditorSwitcherTests"
  </acceptance_criteria>
  <done>Project scaffold and test target exist with the expected names.</done>
</task>

<task type="auto">
  <name>Task 2: Define the developer-strict file taxonomy and UTType resolver</name>
  <files>App/Domain/Types/FileScope.swift, App/Domain/Types/LanguageBucket.swift, App/Domain/Types/ContentTypeResolver.swift</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-discovery-association-core/01-CONTEXT.md, .planning/phases/01-discovery-association-core/01-RESEARCH.md</read_first>
  <action>Create `App/Domain/Types/LanguageBucket.swift` with `enum LanguageBucket: String, CaseIterable` covering exactly `python`, `web`, `go`, `java`, `rust`, and `markdown`. Create `App/Domain/Types/FileScope.swift` with cases for `.allText`, `.language(LanguageBucket)`, and `.customExtensions(Set<String>)`. Create `App/Domain/Types/ContentTypeResolver.swift` with a `DeveloperTextCatalog` or equivalent static source of truth that includes the global developer-strict baseline extensions `txt`, `md`, `mdx`, `json`, `yaml`, `yml`, `toml`, `xml`, `csv`, `log`, `ini`, `conf`, `cfg`, `env`, `sh`, `zsh`, `bash`, `fish`, `py`, `js`, `jsx`, `ts`, `tsx`, `go`, `java`, `rs`, `html`, `css`, `vue`, and `svelte`. Add APIs that normalize extensions to lowercase, resolve `UTType(filenameExtension:)`, and expose booleans for `isDeclared`, `conformsToText`, and `conformsToSourceCode`.</action>
  <verify>rg -n "case python|case web|case go|case java|case rust|case markdown|txt|mdx|svelte|UTType\\(filenameExtension:" App/Domain/Types/FileScope.swift App/Domain/Types/LanguageBucket.swift App/Domain/Types/ContentTypeResolver.swift</verify>
  <acceptance_criteria>
    - App/Domain/Types/LanguageBucket.swift contains "case python", "case web", "case go", "case java", "case rust", and "case markdown"
    - App/Domain/Types/FileScope.swift contains "case allText", "case language", and "case customExtensions"
    - App/Domain/Types/ContentTypeResolver.swift contains "txt", "mdx", "tsx", "svelte", and "UTType(filenameExtension:"
    - App/Domain/Types/ContentTypeResolver.swift contains "conforms(to: .text)" or equivalent text-conformance check
  </acceptance_criteria>
  <done>The phase has a single source of truth for global text scope and language buckets.</done>
</task>

<task type="auto">
  <name>Task 3: Add unit tests for the taxonomy baseline</name>
  <files>Tests/DefaultEditorSwitcherTests/FileScopeCatalogTests.swift</files>
  <read_first>App/Domain/Types/FileScope.swift, App/Domain/Types/LanguageBucket.swift, App/Domain/Types/ContentTypeResolver.swift, .planning/phases/01-discovery-association-core/01-VALIDATION.md</read_first>
  <action>Create `Tests/DefaultEditorSwitcherTests/FileScopeCatalogTests.swift` and add test cases that assert: 1) the global developer-text scope contains `py`, `ts`, `rs`, `md`, and `json`; 2) `LanguageBucket.markdown` resolves `md` and `mdx`; 3) `LanguageBucket.web` resolves `html`, `css`, `js`, `jsx`, `ts`, `tsx`, `vue`, and `svelte`; and 4) extension normalization lowercases uppercase input before lookup. Use XCTest and keep test names descriptive, such as `testAllTextIncludesSourceCodeExtensions()` and `testWebBucketContainsExpectedExtensions()`.</action>
  <verify>rg -n "testAllTextIncludesSourceCodeExtensions|testWebBucketContainsExpectedExtensions|py|tsx|svelte|mdx" Tests/DefaultEditorSwitcherTests/FileScopeCatalogTests.swift</verify>
  <acceptance_criteria>
    - Tests/DefaultEditorSwitcherTests/FileScopeCatalogTests.swift exists
    - FileScopeCatalogTests.swift contains "testAllTextIncludesSourceCodeExtensions"
    - FileScopeCatalogTests.swift contains "testWebBucketContainsExpectedExtensions"
    - FileScopeCatalogTests.swift contains "mdx" and "svelte"
  </acceptance_criteria>
  <done>Taxonomy behavior is guarded by repeatable tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f DefaultEditorSwitcher.xcodeproj/project.pbxproj` succeeds
- [ ] `rg -n "UTType\\(filenameExtension:" App/Domain/Types/ContentTypeResolver.swift` succeeds
- [ ] `xcodebuild test -scheme DefaultEditorSwitcher -destination 'platform=macOS' -only-testing:DefaultEditorSwitcherTests/FileScopeCatalogTests` succeeds
</verification>

<success_criteria>

- All tasks completed
- The project builds with an app target and unit-test target
- The developer-strict taxonomy is encoded in source, not hidden in docs
- Unit tests prove source-code extensions are included in the global text baseline
  </success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-association-core/01-01-SUMMARY.md`
</output>

---
phase: 01-discovery-association-core
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - App/Domain/Associations/PreferredHandler.swift
  - App/Domain/Associations/AssociationVerificationResult.swift
  - App/Infrastructure/LaunchServices/LaunchServicesClient.swift
  - App/Infrastructure/LaunchServices/LaunchServicesAssociationVerifier.swift
  - Tools/AssociationProbe/main.swift
  - DefaultEditorSwitcher.xcodeproj/project.pbxproj
  - Tests/DefaultEditorSwitcherTests/LaunchServicesClientTests.swift
autonomous: true
requirements:
  - DISC-03
user_setup: []
must_haves:
  truths:
    - The app can read current preferred editor handlers and request a new editor handler for a content type through a dedicated Launch Services client.
    - Every association write returns a structured verification result that distinguishes matched, mismatched, and unsupported outcomes.
    - The repository contains a concrete smoke-probe path for testing a real write + readback cycle on representative text/source-code types.
  artifacts:
    - App/Infrastructure/LaunchServices/LaunchServicesClient.swift
    - App/Infrastructure/LaunchServices/LaunchServicesAssociationVerifier.swift
    - App/Domain/Associations/AssociationVerificationResult.swift
    - Tools/AssociationProbe/main.swift
    - Tests/DefaultEditorSwitcherTests/LaunchServicesClientTests.swift
  key_links:
    - LaunchServicesClient bridges UTType identifiers into CoreServices role-handler APIs.
    - LaunchServicesAssociationVerifier consumes LaunchServicesClient read/write calls and emits AssociationVerificationResult values used by later UX phases.
    - AssociationProbe exercises the same LaunchServicesClient code path used by the app.
---

<objective>
Implement the Launch Services association layer, readback verification model, and a concrete smoke-probe path for real-machine validation.

Purpose: Phase 1 is only complete if the project can safely reason about requested versus effective handlers instead of assuming writes worked.
Output: Launch Services client wrappers, verification result types, probe entry point, and tests for the read/write core.
</objective>

<execution_context>
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-discovery-association-core/01-CONTEXT.md
@.planning/phases/01-discovery-association-core/01-RESEARCH.md
@.planning/phases/01-discovery-association-core/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Launch Services read/write adapters and result models</name>
  <files>App/Domain/Associations/PreferredHandler.swift, App/Domain/Associations/AssociationVerificationResult.swift, App/Infrastructure/LaunchServices/LaunchServicesClient.swift, App/Infrastructure/LaunchServices/LaunchServicesAssociationVerifier.swift</files>
  <read_first>App/Domain/Types/ContentTypeResolver.swift, App/Domain/Editors/EditorCandidate.swift, .planning/phases/01-discovery-association-core/01-RESEARCH.md, .planning/phases/01-discovery-association-core/01-CONTEXT.md</read_first>
  <action>Create `App/Domain/Associations/PreferredHandler.swift` to model the requested bundle ID, effective bundle ID, content-type identifier, and role. Create `App/Domain/Associations/AssociationVerificationResult.swift` with explicit cases or statuses for `matched`, `mismatched`, `unsupportedTarget`, and `writeFailed`. Implement `App/Infrastructure/LaunchServices/LaunchServicesClient.swift` with methods named `currentEditorBundleID(for:)`, `allEditorBundleIDs(for:)`, and `setDefaultEditor(bundleID:for:)` that call `LSCopyDefaultRoleHandlerForContentType`, `LSCopyAllRoleHandlersForContentType`, and `LSSetDefaultRoleHandlerForContentType` using `LSRolesMask.editor`. Implement `App/Infrastructure/LaunchServices/LaunchServicesAssociationVerifier.swift` so a write attempt always performs a readback and returns an `AssociationVerificationResult` containing both the requested and effective bundle IDs.</action>
  <verify>rg -n "LSCopyDefaultRoleHandlerForContentType|LSCopyAllRoleHandlersForContentType|LSSetDefaultRoleHandlerForContentType|matched|mismatched|unsupportedTarget|writeFailed" App/Infrastructure/LaunchServices/LaunchServicesClient.swift App/Infrastructure/LaunchServices/LaunchServicesAssociationVerifier.swift App/Domain/Associations/AssociationVerificationResult.swift</verify>
  <acceptance_criteria>
    - LaunchServicesClient.swift contains "LSCopyDefaultRoleHandlerForContentType"
    - LaunchServicesClient.swift contains "LSCopyAllRoleHandlersForContentType"
    - LaunchServicesClient.swift contains "LSSetDefaultRoleHandlerForContentType"
    - AssociationVerificationResult.swift contains "matched", "mismatched", "unsupportedTarget", and "writeFailed"
    - LaunchServicesAssociationVerifier.swift contains both requested and effective bundle identifiers in the result model
  </acceptance_criteria>
  <done>The codebase can request and verify preferred-handler changes through one coherent service layer.</done>
</task>

<task type="auto">
  <name>Task 2: Add an executable smoke probe for representative content types</name>
  <files>Tools/AssociationProbe/main.swift, DefaultEditorSwitcher.xcodeproj/project.pbxproj</files>
  <read_first>App/Infrastructure/LaunchServices/LaunchServicesClient.swift, App/Infrastructure/LaunchServices/LaunchServicesAssociationVerifier.swift, .planning/phases/01-discovery-association-core/01-VALIDATION.md</read_first>
  <action>Add a separate executable target named `AssociationProbe` to `DefaultEditorSwitcher.xcodeproj/project.pbxproj` and place its entry point at `Tools/AssociationProbe/main.swift`. In `main.swift`, parse `--extension`, `--bundle-id`, and optional `--restore-bundle-id` arguments; resolve the extension through `UTType(filenameExtension:)`; call the shared `LaunchServicesAssociationVerifier`; print the requested bundle ID, effective bundle ID, and verification status; and, when `--restore-bundle-id` is provided, restore the previous handler after the probe completes. Keep the output machine-readable with labels `requested=`, `effective=`, and `status=`.</action>
  <verify>rg -n "AssociationProbe|--extension|--bundle-id|--restore-bundle-id|requested=|effective=|status=" Tools/AssociationProbe/main.swift DefaultEditorSwitcher.xcodeproj/project.pbxproj</verify>
  <acceptance_criteria>
    - Tools/AssociationProbe/main.swift exists
    - Tools/AssociationProbe/main.swift contains "--extension", "--bundle-id", and "--restore-bundle-id"
    - Tools/AssociationProbe/main.swift contains "requested=", "effective=", and "status="
    - DefaultEditorSwitcher.xcodeproj/project.pbxproj contains "AssociationProbe"
  </acceptance_criteria>
  <done>A real-machine smoke path exists for write + readback verification without waiting for the future app UI.</done>
</task>

<task type="auto">
  <name>Task 3: Add adapter tests for verification outcomes</name>
  <files>Tests/DefaultEditorSwitcherTests/LaunchServicesClientTests.swift</files>
  <read_first>App/Domain/Associations/PreferredHandler.swift, App/Domain/Associations/AssociationVerificationResult.swift, App/Infrastructure/LaunchServices/LaunchServicesClient.swift, App/Infrastructure/LaunchServices/LaunchServicesAssociationVerifier.swift, .planning/phases/01-discovery-association-core/01-VALIDATION.md</read_first>
  <action>Create `Tests/DefaultEditorSwitcherTests/LaunchServicesClientTests.swift` with mock-backed tests that assert: 1) a successful set followed by identical readback returns `.matched`; 2) a readback mismatch returns `.mismatched` with both bundle IDs preserved; 3) a missing handler declaration or unsupported target returns `.unsupportedTarget`; and 4) a simulated Launch Services error maps to `.writeFailed`. Add one test name per path, including `testVerificationReturnsMatchedWhenReadbackMatchesRequestedHandler()` and `testVerificationReturnsMismatchedWhenEffectiveHandlerDiffers()`.</action>
  <verify>rg -n "testVerificationReturnsMatchedWhenReadbackMatchesRequestedHandler|testVerificationReturnsMismatchedWhenEffectiveHandlerDiffers|unsupportedTarget|writeFailed" Tests/DefaultEditorSwitcherTests/LaunchServicesClientTests.swift</verify>
  <acceptance_criteria>
    - Tests/DefaultEditorSwitcherTests/LaunchServicesClientTests.swift exists
    - LaunchServicesClientTests.swift contains "testVerificationReturnsMatchedWhenReadbackMatchesRequestedHandler"
    - LaunchServicesClientTests.swift contains "testVerificationReturnsMismatchedWhenEffectiveHandlerDiffers"
    - LaunchServicesClientTests.swift contains "unsupportedTarget" and "writeFailed"
  </acceptance_criteria>
  <done>Association read/write behavior is covered for the primary success and failure states.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "LSSetDefaultRoleHandlerForContentType|LSCopyDefaultRoleHandlerForContentType" App/Infrastructure/LaunchServices/LaunchServicesClient.swift` succeeds
- [ ] `xcodebuild build -scheme AssociationProbe -destination 'platform=macOS'` succeeds
- [ ] `xcodebuild test -scheme DefaultEditorSwitcher -destination 'platform=macOS' -only-testing:DefaultEditorSwitcherTests/LaunchServicesClientTests` succeeds
</verification>

<success_criteria>

- All tasks completed
- Launch Services writes are represented as requested-versus-effective outcomes, not fire-and-forget calls
- The repository contains a runnable probe for representative type mutation/readback
- Tests cover matched, mismatched, unsupported, and write-failed outcomes
  </success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-association-core/01-03-SUMMARY.md`
</output>

---
phase: 01-discovery-association-core
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - App/Support/KnownEditors.swift
  - App/Domain/Editors/EditorCandidate.swift
  - App/Domain/Editors/EditorRankingPolicy.swift
  - App/Infrastructure/Workspace/BundleDocumentTypeReader.swift
  - App/Infrastructure/Workspace/WorkspaceAppDiscovery.swift
  - Tests/DefaultEditorSwitcherTests/EditorRankingPolicyTests.swift
  - Tests/DefaultEditorSwitcherTests/WorkspaceDiscoveryTests.swift
autonomous: true
requirements:
  - DISC-01
  - DISC-02
user_setup: []
must_haves:
  truths:
    - The app can produce editor candidates from system-discovered applications for a requested content type.
    - Every editor candidate carries a bundle identifier, display name, icon lookup path or placeholder, and recommendation metadata.
    - Recommended editors are ranked ahead of other eligible applications without hiding the full eligible set.
  artifacts:
    - App/Support/KnownEditors.swift
    - App/Domain/Editors/EditorCandidate.swift
    - App/Domain/Editors/EditorRankingPolicy.swift
    - App/Infrastructure/Workspace/WorkspaceAppDiscovery.swift
    - Tests/DefaultEditorSwitcherTests/WorkspaceDiscoveryTests.swift
  key_links:
    - WorkspaceAppDiscovery merges NSWorkspace results with KnownEditors weights through EditorRankingPolicy.
    - WorkspaceDiscoveryTests verify both metadata capture and recommendation ordering.
---

<objective>
Implement the system-backed editor discovery pipeline and hybrid ranking model for recommended versus other eligible apps.

Purpose: Later switching UI and rule management depend on a reliable, developer-friendly editor catalog rather than raw system order.
Output: Known-editor definitions, candidate/ranking domain models, workspace discovery services, and tests that prove recommendation ordering.
</objective>

<execution_context>
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-discovery-association-core/01-CONTEXT.md
@.planning/phases/01-discovery-association-core/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Encode the curated known-editor catalog and ranking policy</name>
  <files>App/Support/KnownEditors.swift, App/Domain/Editors/EditorCandidate.swift, App/Domain/Editors/EditorRankingPolicy.swift</files>
  <read_first>App/Domain/Types/FileScope.swift, App/Domain/Types/LanguageBucket.swift, .planning/phases/01-discovery-association-core/01-CONTEXT.md, .planning/phases/01-discovery-association-core/01-RESEARCH.md</read_first>
  <action>Create `App/Support/KnownEditors.swift` with a `KnownEditor` model containing `bundleID`, `displayName`, `globalWeight`, and `bucketWeights`. Seed the catalog with explicit entries for `com.microsoft.VSCode`, `com.sublimetext.4`, `com.macromates.TextMate`, `com.coteditor.CotEditor`, `com.barebones.bbedit`, `com.jetbrains.intellij`, `com.jetbrains.pycharm`, `com.jetbrains.webstorm`, `com.jetbrains.goland`, and `com.jetbrains.rustrover`. Create `App/Domain/Editors/EditorCandidate.swift` with fields for `bundleID`, `displayName`, `source`, `capability`, and `isRecommended`. Create `App/Domain/Editors/EditorRankingPolicy.swift` so ranking first groups recommended editors ahead of non-recommended editors, then sorts by descending `globalWeight`, then applies bucket-specific bonuses for `python`, `web`, `go`, `java`, `rust`, and `markdown`.</action>
  <verify>rg -n "com\\.microsoft\\.VSCode|com\\.jetbrains\\.pycharm|globalWeight|bucketWeights|isRecommended" App/Support/KnownEditors.swift App/Domain/Editors/EditorCandidate.swift App/Domain/Editors/EditorRankingPolicy.swift</verify>
  <acceptance_criteria>
    - App/Support/KnownEditors.swift contains "com.microsoft.VSCode" and "com.jetbrains.pycharm"
    - App/Support/KnownEditors.swift contains "globalWeight" and "bucketWeights"
    - App/Domain/Editors/EditorCandidate.swift contains "bundleID", "displayName", "capability", and "isRecommended"
    - App/Domain/Editors/EditorRankingPolicy.swift contains logic for "python", "web", "go", "java", "rust", and "markdown"
  </acceptance_criteria>
  <done>The codebase has an explicit curated editor ranking model that later UI phases can consume directly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement NSWorkspace-based discovery with metadata extraction</name>
  <files>App/Infrastructure/Workspace/BundleDocumentTypeReader.swift, App/Infrastructure/Workspace/WorkspaceAppDiscovery.swift</files>
  <read_first>App/Support/KnownEditors.swift, App/Domain/Editors/EditorCandidate.swift, App/Domain/Types/ContentTypeResolver.swift, .planning/phases/01-discovery-association-core/01-RESEARCH.md</read_first>
  <action>Create `App/Infrastructure/Workspace/BundleDocumentTypeReader.swift` that reads `CFBundleDocumentTypes`, `LSItemContentTypes`, `CFBundleTypeRole`, and `LSHandlerRank` from an app bundle and normalizes them into a metadata structure keyed by bundle ID. Create `App/Infrastructure/Workspace/WorkspaceAppDiscovery.swift` that calls `NSWorkspace.shared.urlsForApplications(toOpen:)`, converts each result into an `EditorCandidate`, loads icon and display name via bundle APIs, marks the source as either `.recommendedCatalog` or `.systemEligible`, and marks capability as `.full`, `.partial`, or `.unverified` based on whether the bundle declarations fully cover the requested extension/type set.</action>
  <verify>rg -n "CFBundleDocumentTypes|LSItemContentTypes|CFBundleTypeRole|LSHandlerRank|urlsForApplications\\(toOpen:" App/Infrastructure/Workspace/BundleDocumentTypeReader.swift App/Infrastructure/Workspace/WorkspaceAppDiscovery.swift</verify>
  <acceptance_criteria>
    - BundleDocumentTypeReader.swift contains "CFBundleDocumentTypes", "LSItemContentTypes", "CFBundleTypeRole", and "LSHandlerRank"
    - WorkspaceAppDiscovery.swift contains "urlsForApplications(toOpen:"
    - WorkspaceAppDiscovery.swift contains ".full", ".partial", and ".unverified"
    - WorkspaceAppDiscovery.swift maps both display name and bundle identifier into EditorCandidate values
  </acceptance_criteria>
  <done>The app can discover and normalize candidate editors from the workspace and bundle metadata.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for ranking order and discovery metadata</name>
  <files>Tests/DefaultEditorSwitcherTests/EditorRankingPolicyTests.swift, Tests/DefaultEditorSwitcherTests/WorkspaceDiscoveryTests.swift</files>
  <read_first>App/Support/KnownEditors.swift, App/Domain/Editors/EditorRankingPolicy.swift, App/Infrastructure/Workspace/BundleDocumentTypeReader.swift, App/Infrastructure/Workspace/WorkspaceAppDiscovery.swift, .planning/phases/01-discovery-association-core/01-VALIDATION.md</read_first>
  <action>Create `Tests/DefaultEditorSwitcherTests/EditorRankingPolicyTests.swift` with tests that prove recommended editors sort ahead of non-recommended candidates and that Python/Web bucket bonuses can change order within the recommended tier. Create `Tests/DefaultEditorSwitcherTests/WorkspaceDiscoveryTests.swift` with fixture-backed tests for bundle metadata parsing and candidate capability classification. Use explicit fixture dictionaries containing `CFBundleDocumentTypes`, `LSItemContentTypes`, `CFBundleTypeRole`, and `LSHandlerRank` so discovery logic is testable without relying on installed apps.</action>
  <verify>rg -n "recommended|python|web|CFBundleDocumentTypes|LSItemContentTypes|partial|unverified" Tests/DefaultEditorSwitcherTests/EditorRankingPolicyTests.swift Tests/DefaultEditorSwitcherTests/WorkspaceDiscoveryTests.swift</verify>
  <acceptance_criteria>
    - Tests/DefaultEditorSwitcherTests/EditorRankingPolicyTests.swift exists
    - EditorRankingPolicyTests.swift contains assertions for recommended-first ordering
    - Tests/DefaultEditorSwitcherTests/WorkspaceDiscoveryTests.swift exists
    - WorkspaceDiscoveryTests.swift contains "CFBundleDocumentTypes", "LSItemContentTypes", "partial", and "unverified"
  </acceptance_criteria>
  <done>Discovery behavior and ranking logic are proven by deterministic tests rather than local machine state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "com\\.microsoft\\.VSCode|com\\.jetbrains\\.pycharm" App/Support/KnownEditors.swift` succeeds
- [ ] `rg -n "urlsForApplications\\(toOpen:" App/Infrastructure/Workspace/WorkspaceAppDiscovery.swift` succeeds
- [ ] `xcodebuild test -scheme DefaultEditorSwitcher -destination 'platform=macOS' -only-testing:DefaultEditorSwitcherTests/EditorRankingPolicyTests -only-testing:DefaultEditorSwitcherTests/WorkspaceDiscoveryTests` succeeds
</verification>

<success_criteria>

- All tasks completed
- Discovery results include bundle identifiers, display names, and recommendation metadata
- Recommended editors sort before other eligible apps
- Tests cover both ranking policy and bundle metadata parsing
  </success_criteria>

<output>
After completion, create `.planning/phases/01-discovery-association-core/01-02-SUMMARY.md`
</output>

---
phase: 02-menu-bar-global-switch
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - App/Application/GlobalText/GlobalTextSwitchReport.swift
  - App/Application/GlobalText/GlobalTextSwitchCoordinator.swift
  - App/Features/MenuBar/MenuBarViewModel.swift
  - Tests/DefaultEditorSwitcherTests/GlobalTextSwitchCoordinatorTests.swift
  - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift
autonomous: true
requirements:
  - GLOB-01
  - GLOB-03
user_setup: []
must_haves:
  truths:
    - A requested editor can be applied to every declared content type in the developer-text scope through one dedicated coordinator.
    - The apply path returns a structured aggregate report with counts for matched, mismatched, unsupported, and write-failed results.
    - The menu view model consumes that report and refreshes current state instead of assuming the write succeeded everywhere.
  artifacts:
    - App/Application/GlobalText/GlobalTextSwitchCoordinator.swift
    - App/Application/GlobalText/GlobalTextSwitchReport.swift
    - App/Features/MenuBar/MenuBarViewModel.swift
    - Tests/DefaultEditorSwitcherTests/GlobalTextSwitchCoordinatorTests.swift
  key_links:
    - GlobalTextSwitchCoordinator expands `.allText` using ContentTypeResolver and delegates each type to LaunchServicesAssociationVerifier.
    - GlobalTextSwitchReport translates raw AssociationVerificationResult values into counts and sample failure details that the menu can display directly.
    - MenuBarViewModel calls the coordinator, stores the latest report, and reloads current state after apply.
---

<objective>
Wire the global text switch action through the verified Phase 1 association writer and expose structured post-apply state to the menu layer.

Purpose: The core value of the product is one-click switching of the whole developer-text scope, so the app needs a real batch-apply coordinator rather than per-row placeholder actions.
Output: A global switch coordinator, an aggregate report model for UI feedback, and tests proving multi-type writes and readback statuses are surfaced accurately.
</objective>

<execution_context>
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/workflows/execute-plan.md
@/Users/congbo/workspace/default-editor-switcher/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-menu-bar-global-switch/02-CONTEXT.md
@.planning/phases/02-menu-bar-global-switch/02-RESEARCH.md
@.planning/phases/02-menu-bar-global-switch/02-UI-SPEC.md
@.planning/phases/02-menu-bar-global-switch/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the global-text switch coordinator and aggregate report model</name>
  <files>App/Application/GlobalText/GlobalTextSwitchReport.swift, App/Application/GlobalText/GlobalTextSwitchCoordinator.swift</files>
  <read_first>App/Domain/Associations/AssociationVerificationResult.swift, App/Infrastructure/LaunchServices/LaunchServicesAssociationVerifier.swift, App/Domain/Types/ContentTypeResolver.swift, .planning/phases/02-menu-bar-global-switch/02-RESEARCH.md</read_first>
  <action>Create `App/Application/GlobalText/GlobalTextSwitchReport.swift` with explicit fields for `requestedBundleID`, `matchedCount`, `mismatchedCount`, `unsupportedCount`, `writeFailedCount`, `processedContentTypeIdentifiers`, and `sampleFailures`. Create `App/Application/GlobalText/GlobalTextSwitchCoordinator.swift` so `apply(bundleID:)` resolves `ContentTypeResolver.resolutions(for: .allText)`, filters out undeclared or nil `UTType` entries, calls `LaunchServicesAssociationVerifier.verify(requestedBundleID:for:)` for each remaining type, and collapses the returned results into one `GlobalTextSwitchReport`. Preserve representative failure details such as content-type identifier and result status instead of flattening everything into a Boolean.</action>
  <verify>rg -n "matchedCount|mismatchedCount|unsupportedCount|writeFailedCount|resolutions\\(for: \\.allText\\)|verify\\(requestedBundleID:" App/Application/GlobalText/GlobalTextSwitchReport.swift App/Application/GlobalText/GlobalTextSwitchCoordinator.swift</verify>
  <acceptance_criteria>
    - App/Application/GlobalText/GlobalTextSwitchReport.swift contains "matchedCount", "mismatchedCount", "unsupportedCount", and "writeFailedCount"
    - App/Application/GlobalText/GlobalTextSwitchCoordinator.swift contains "resolutions(for: .allText)"
    - App/Application/GlobalText/GlobalTextSwitchCoordinator.swift contains "verify(requestedBundleID:"
    - GlobalTextSwitchCoordinator filters out undeclared or nil UTType resolutions before applying writes
  </acceptance_criteria>
  <done>The codebase has one dedicated global-switch service that can explain what happened across the whole scope.</done>
</task>

<task type="auto">
  <name>Task 2: Connect menu actions to the switch coordinator and refresh cycle</name>
  <files>App/Features/MenuBar/MenuBarViewModel.swift</files>
  <read_first>App/Features/MenuBar/MenuBarViewModel.swift, App/Application/GlobalText/GlobalTextStateService.swift, App/Application/GlobalText/GlobalTextSwitchCoordinator.swift, .planning/phases/02-menu-bar-global-switch/02-UI-SPEC.md</read_first>
  <action>Update `App/Features/MenuBar/MenuBarViewModel.swift` to inject `GlobalTextSwitchCoordinator` alongside `GlobalTextStateService`. Add an async or callback-based `applyEditor(bundleID:)` action that marks the row as busy, calls the coordinator, stores the latest `GlobalTextSwitchReport`, and then immediately re-runs `load()` so the summary and inline `Current` marker are read back from system state. Keep the recommended editor rows as one-click actions with labels in the exact `Use {EditorName} for All Text Files` copy contract; do not insert a confirmation step in Phase 02.</action>
  <verify>rg -n "applyEditor|GlobalTextSwitchCoordinator|GlobalTextSwitchReport|Use .* for All Text Files|load\\(" App/Features/MenuBar/MenuBarViewModel.swift</verify>
  <acceptance_criteria>
    - App/Features/MenuBar/MenuBarViewModel.swift contains "applyEditor"
    - MenuBarViewModel.swift contains "GlobalTextSwitchCoordinator"
    - MenuBarViewModel.swift stores a "GlobalTextSwitchReport" or equivalent last-apply model
    - MenuBarViewModel.swift refreshes current state by calling "load()" after apply
  </acceptance_criteria>
  <done>The menu action path now performs real global writes and immediately refreshes its state from the machine.</done>
</task>

<task type="auto">
  <name>Task 3: Add tests for batch apply coverage and report aggregation</name>
  <files>Tests/DefaultEditorSwitcherTests/GlobalTextSwitchCoordinatorTests.swift, Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift</files>
  <read_first>App/Application/GlobalText/GlobalTextSwitchCoordinator.swift, App/Application/GlobalText/GlobalTextSwitchReport.swift, App/Features/MenuBar/MenuBarViewModel.swift, .planning/phases/02-menu-bar-global-switch/02-VALIDATION.md</read_first>
  <action>Create `Tests/DefaultEditorSwitcherTests/GlobalTextSwitchCoordinatorTests.swift` with tests named `testApplyWritesAllDeclaredTypes()` and `testAggregateReportCountsMatchedAndFailedResults()` using a fake verifier so the coordinator can be exercised without mutating Launch Services. Extend `Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift` with a test that proves `applyEditor(bundleID:)` stores the latest aggregate report and triggers a reload of current state after the apply completes.</action>
  <verify>rg -n "testApplyWritesAllDeclaredTypes|testAggregateReportCountsMatchedAndFailedResults|applyEditor" Tests/DefaultEditorSwitcherTests/GlobalTextSwitchCoordinatorTests.swift Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift</verify>
  <acceptance_criteria>
    - Tests/DefaultEditorSwitcherTests/GlobalTextSwitchCoordinatorTests.swift exists
    - GlobalTextSwitchCoordinatorTests.swift contains "testApplyWritesAllDeclaredTypes"
    - GlobalTextSwitchCoordinatorTests.swift contains "testAggregateReportCountsMatchedAndFailedResults"
    - Tests/DefaultEditorSwitcherTests/MenuBarViewModelTests.swift verifies that apply triggers a reload
  </acceptance_criteria>
  <done>The write path is covered by repeatable tests before UI polish and window handoff are added.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "matchedCount|writeFailedCount" App/Application/GlobalText/GlobalTextSwitchReport.swift` succeeds
- [ ] `rg -n "applyEditor|load\\(" App/Features/MenuBar/MenuBarViewModel.swift` succeeds
- [ ] `xcodebuild test -scheme DefaultEditorSwitcher -destination 'platform=macOS' -only-testing:DefaultEditorSwitcherTests/GlobalTextSwitchCoordinatorTests -only-testing:DefaultEditorSwitcherTests/MenuBarViewModelTests` succeeds
</verification>

<success_criteria>

- All tasks completed
- One menu action applies the requested editor to the entire declared developer-text scope
- Post-apply feedback can distinguish matched, mismatched, unsupported, and write-failed outcomes
- The menu refreshes from system state instead of trusting stale optimistic state
  </success_criteria>

<output>
After completion, create `.planning/phases/02-menu-bar-global-switch/02-02-SUMMARY.md`
</output>
